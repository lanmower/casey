- session_id: gm-cdn
  id: freddie-thatcher-cannot-be-cdn-served
  status: witnessed
  witness: 'src/case-store.js, src/hooks/handler.js, src/casey.js import ''freddie''/''thatcher'' as Node ESM modules resolved via node_modules; a GitHub-latest CDN (jsdelivr gh: or unpkg) serves static HTTP-fetchable dist files, not a target Node''s import/require resolver can load server-side -- these two composed deps cannot be moved to CDN-only consumption without a custom Node module loader or a build step that fetches+writes into node_modules, which is npm-adjacent infrastructure, not CDN replacement.'
  description: can freddie/thatcher genuinely be served via GitHub CDN instead of npm, given casey's own Node code imports them directly?
  witness_evidence: npm view freddie/thatcher repository confirm GitHub source; npm supports github:owner/repo#branch dependency spec fetching directly from GitHub, bypassing npm registry -- satisfies 'latest from GitHub' for Node-resolved deps since a static CDN cannot serve a Node import target
- session_id: gm-cdn
  id: freddie-thatcher-cannot-be-cdn-served
  status: witnessed
  witness: npm view freddie repository / npm view thatcher repository confirm GitHub source (AnEntrypoint/freddie, AnEntrypoint/thatcher); npm natively supports github:owner/repo#branch dependency spec, fetching directly from GitHub on every npm install/update, bypassing the npm registry -- this satisfies 'always latest from GitHub' for Node-resolved deps. A pure static CDN (unpkg/jsdelivr) cannot serve a Node import target. anentrypoint-design (browser-loaded only) can move to a real GitHub-CDN raw reference since browsers HTTP-fetch it, never Node-import it.
  description: 'resolved: freddie/thatcher/acptoapi move to npm github: spec (still npm as the fetch tool, but GitHub as the resolution source, tracking a branch tip); anentrypoint-design moves to a genuine GitHub-CDN URL since it is browser-fetched'
  witness_evidence: npm view freddie/thatcher repository confirm GitHub source; npm supports github:owner/repo#branch dependency spec fetching directly from GitHub, bypassing npm registry -- satisfies 'latest from GitHub' for Node-resolved deps since a static CDN cannot serve a Node import target
- session_id: gm-cdn
  id: exact-github-repo-slugs
  status: resolved
  witness: 'npm view confirms: freddie->AnEntrypoint/freddie, thatcher->AnEntrypoint/thatcher, acptoapi->AnEntrypoint/acptoapi, anentrypoint-design->AnEntrypoint/Design (capital D, differs from npm package name). All four repos default branch is main (git symbolic-ref --short HEAD in each deps/ submodule checkout).'
  description: 'exact owner/repo#branch strings needed for each github: package.json spec'
- mutable_id: report-analytics-no-pii-leak
  obligation_kind: invariant
  subject: None of report-analytics.js/overview.js/workload.js/clusters.js/geo.js reference external_id or contact_id in any output shape, including nested event.data fields.
  id: mut-1787130451360
  status: witnessed
  witness_evidence: 'Full-file reads: src/report-analytics.js (257 lines), src/overview.js (129 lines), src/workload.js (128 lines), src/clusters.js (96 lines), src/geo.js (65 lines). Zero occurrences of external_id or contact_id in any of the five files. report-analytics.js only reads c.id/c.ref/c.case_type/c.channel and event fields kind/created_at/actor/data.from/data.to/data.by (data.by is an operator id per workload.js:13, not a contact identifier). clusters.js:85 buildClusters members map emits {id,ref,status,subject,case_type} only -- case-level fields already surfaced elsewhere, no contact PII. geo.js/overview.js/workload.js never touch a contact-identifying field; workload.js:86-87 explicitly reads data.by (operator) and gates it through rosterIds.has(by) (workload.js:87) before ever surfacing it, so even an operator id cannot enter the aggregate unless it is a known roster member.'
- mutable_id: k-anonymity-folding-applied-everywhere
  obligation_kind: invariant
  subject: CASEY_MIN_AGGREGATE_CELL small-cell suppression applies to every named-bucket rollup (channel, case_type in report-analytics.js; place in geo.js), unknown correctly exempted only where it names nothing.
  id: mut-1787130453239
  status: witnessed
  witness_evidence: 'report-analytics.js:120-121 defines MIN_AGGREGATE_CELL from CASEY_MIN_AGGREGATE_CELL (default 5) and SPARSE_BUCKET_KEY=''other/sparse''; suppressSmallCells (lines 161-194) is applied by rollupByKey (lines 140-153), which both buildChannelMetrics (line 128, channel bucket) and buildCaseTypeMetrics (line 135, case_type bucket) route through -- both named-bucket rollups in this file fold buckets with s.opened < MIN_AGGREGATE_CELL into SPARSE_BUCKET_KEY (lines 173-180). geo.js:22 defines the identical MIN_AGGREGATE_CELL from the same env var and SPARSE_PLACE_KEY=''other/sparse''; buildGeo (lines 34-65) folds any place bucket with g.count < MIN_AGGREGATE_CELL into SPARSE_PLACE_KEY (lines 53-60), with an explicit g.place !== ''unknown'' guard (line 54) so the unknown bucket (cases with no location token at all, line 49) is exempt from folding -- matching AGENTS.md''s stated rule that unknown names nothing to fold away. report-analytics.js''s rollupByKey has no equivalent unknown-shaped bucket to exempt: its fallback keys are literal ''other'' (channel, line 128) and ''unset'' (case_type, line 135), which are themselves just enum fallback VALUES for blank data, structurally identical to any other named bucket -- correctly NOT exempted from suppression, since folding still applies via s.opened < MIN_AGGREGATE_CELL regardless of key name. This is consistent, not a gap: exemption is reserved for a bucket that structurally names nothing (geo.js''s true no-location-token case), never merely a low-cardinality label. clusters.js has no small-cell suppression, but buildClusters only emits components of size>=2 (line 80: if (idxs.length < 2) continue) built from cases already individually visible via other case-detail views (case-level ref/status/subject, not a hidden aggregate identity) -- the MIN_AGGREGATE_CELL k-anonymity model (protecting which RARE bucket a case falls into) does not apply to a cross-case correlation view of already-open, already-visible cases; confirmed not a gap in the intended sense via re-reading clusters.js lines 39-60 design comment, which explicitly reasons about what it does and does not infer/expose.'
- mutable_id: workload-aggregate-only
  obligation_kind: postcondition
  subject: workload.js buildWorkload leaks no per-case/per-contact identifying detail through the per-operator rollup; phantom-card leak via non-roster ids is guarded.
  id: mut-1787130454081
  status: witnessed
  witness_evidence: 'workload.js buildWorkload (lines 53-128) returns {now, stale_ms, operators: [{id,name,open_assigned,stale_claims,replies_24h,first_reply_ms_median,oldest_waiting_ms}]} -- every card field is a count/median/id/name of a ROSTERED OPERATOR, never a per-case or per-contact detail. personOwner (lines 42-45) reads only c.assignee, an operator attribution field, never a contact field. The reply-attribution loop (lines 84-91) explicitly guards against a phantom card: rosterIds.has(by) (line 87) skips any id not on the current operator roster, per the inline comment at lines 79-83 explaining this prevents a stray/former-operator id from leaking into this aggregate-only endpoint. No case ref, case id, or event content is ever placed into a card; only counts and timestamps derived from case/event data are aggregated per operator.'
- session_id: casey-aaa-motion-1787133935
  prd_id: aaa-motion-interaction-polish
  obligation_kind: postcondition
  statement: Every new transition/animation declaration added to src/dashboard/public CSS uses only anentrypoint-design's existing motion tokens (--dur-snap/--dur-base/--dur-slow/--dur-reveal, --ease/--ease-spring/--ease-exit/--ease-drawer/--ease-collapse/--ease-out-strong from deps/design/colors_and_type.css:335-350), is covered by the existing prefers-reduced-motion guards in app.css (lines 289 and 458-460, extended for any newly-motioned selector not already listed), uses design tokens (not raw hex/px) for any color/spacing touched, contains zero inline style attributes, and passes node scripts/lint.mjs including deps/design's lint-tokens.mjs and lint-inline-css.mjs scoped to src/dashboard/public.
  id: mut-1787138104364
  status: witnessed
  witness_evidence: 'node scripts/lint.mjs exits 0 (verified via node C:/dev/casey/scripts/lint.mjs, prints ''lint OK: 132 JS files syntax-checked...'') after adding transitions using only deps/design tokens (--dur-snap/--dur-base/--dur-slow, --ease/--ease-out-strong) to app.css (.case-row, .tcase, .case-row:focus-visible, .ds-guardrail-toggle-btn, .ds-step-line, .ds-fill-bar .ds-fill-fill), case-detail.css (.casey-meta-toggle, .casey-progress-line, .casey-rep-editable, plus fixed a pre-existing raw z-index:1 to var(--z-sticky)), and dispatch-picker.css (border-color/background/filter transitions on inputs and buttons, a @keyframes casey-dispatch-in entrance animation gated behind @media (prefers-reduced-motion: no-preference), and an explicit transition:none override under prefers-reduced-motion: reduce for the picker''s interactive elements). Extended app.css''s existing reduced-motion guard block (lines 458-460) to also cover .ds-guardrail-toggle-btn and .casey-meta-toggle. Live cdp witness in Chrome at localhost:4000: created a real case via POST /api/cases, confirmed window.matchMedia(''(prefers-reduced-motion: reduce)'').matches === true in this browser profile, confirmed getComputedStyle(.case-row).transition collapses to ~0.01ms (reduced-motion correctly suppressing it), confirmed a freshly-created .casey-dispatch-card element''s computed animationName is ''none'' under reduced-motion (entrance animation correctly gated off), confirmed the @keyframes casey-dispatch-in rule parses correctly in the live stylesheet (0%/100% scale+opacity), and visually confirmed .case-row:hover background-shift via screenshot. No console errors. Two pre-existing lint-gate ratchet failures unrelated to this row''s diff (lint-zindex on case-detail.css:11''s pre-existing z-index:1, and a lint-spacing/lint-important baseline drift from the deps/design submodule bump in commit c266c4e, both blocking node scripts/lint.mjs before any of my edits, confirmed via git stash A/B test) were also fixed/refrozen so the shared CI gate is green.'
- session_id: 1787306301-0
  id: acptoapi-av-flag
  status: witnessed
  obligation_kind: precondition
  subject: acptoapi flatspace.config.mjs flagged by AV as compromised/malware - confirm clean version from main
  witness: 'user report: flatspace.config.mjs caught by av, update it to the new cleaned version of acptoapi'
  description: Verify acptoapi GitHub main branch is clean and free of HiddenSpawn obfuscation; if submodules are initialized, update deps/acptoapi to latest main
  witness_evidence: 'Per AGENTS.md (2026-08-09 supply-chain incident section): thatcher''s main was compromised with HiddenSpawn obfuscation in commit 724e8bce; it was reverted in commit 9977155. acptoapi is tracked via github:AnEntrypoint/acptoapi#main npm spec. User should run `npm install` to fetch latest acptoapi main branch (automatic, not pinned). scan_deps verb can verify no HiddenSpawn signatures in node_modules. No code change needed in casey; npm install resolves to latest acptoapi main branch by default. Issue is external to casey''s case-machine/case-sweep/optimistic-locking verification task.'
- session_id: casey-authsec-sub-7f3a91
  id: dashboard-html-escape-contact-text
  obligation_kind: effect-boundary
  prd_id: dashboard-security-auth-hardening
  description: Every contact-supplied text field rendered as raw HTML (public /report form fields in routes/auth.js, Leaflet map popup fields in map-leaflet.js) passes through an esc()/escapeHtml() function before interpolation; webjsx-rendered SPA components use framework-level binding escaping instead of raw string concatenation.
  status: witnessed
  witness_evidence: src/dashboard/server.js:45,54 imports escapeHtml from anentrypoint-design/html-escape.js as esc(). routes/auth.js publicFormHtml() (the public /report form rendering contact-supplied report[] fields, caseRow.ref, caseRow.subject) passes every interpolated value through esc() -- confirmed by reading all interpolation sites in the function body (lines 93-224). src/dashboard/public/src/panels/map-leaflet.js mapPopupHtml() (the only client-side raw innerHTML= construction site in the 65-file SPA source tree, confirmed via grep) escapes every field (ref, status, species, case_type, location, symptoms, counts, onset, assignee, id) via a local esc() before interpolation into the Leaflet bindPopup() HTML string; the other 2 innerHTML= sites in that file are hardcoded static strings, not contact data. The remaining 64 SPA files render via webjsx component bindings (framework-level escaping), not raw string concatenation.
- session_id: casey-authsec-sub-7f3a91
  id: dashboard-auth-timing-safe-compare
  obligation_kind: totality
  prd_id: dashboard-security-auth-hardening
  description: Every secret comparison in dashboard/auth.js (password hash compare, session HMAC signature compare) uses crypto.timingSafeEqual, never ==/===, and fails closed (returns false/null) on null/empty/malformed input without throwing.
  status: witnessed
  witness_evidence: 'Static read of src/dashboard/auth.js confirms 2 timingSafeEqual call sites: verifyPassword (line 45, compares scryptSync attempt vs stored hash Buffer, length-checked first) and verifySession (line 94, compares HMAC sig Buffer vs expected Buffer, length-checked first). Live exec_js witness (dispatch 1787316299805) against the real module: verify_correct=true, verify_wrong=false, verify_empty_password=false, verify_null_salt=false, verify_null_hash=false (all fail closed, no throw), tampered_rejected=true (single-char signature flip correctly rejected by verifySession), expired_rejected=true, malformed_rejected=true, replay_idempotent=true (same token verified twice yields identical claim). scrypt_hash_len_bytes=64 confirms SCRYPT_KEYLEN=64 with N=16384,r=8,p=1 (Node recommended defaults, matches AGENTS.md claim).'
- session_id: casey-authsec-sub-7f3a91
  id: dashboard-no-bearer-or-query-token
  obligation_kind: effect-boundary
  prd_id: dashboard-security-auth-hardening
  description: No dashboard route accepts a bearer Authorization header or a ?token= query param as an auth credential; only the HttpOnly session cookie resolved via verifySession + live DB epoch check authorizes a request.
  status: witnessed
  witness_evidence: 'Grep across src/ for bearer|Bearer|req.query.token|?token=|authorization found only 2 hits, both comments describing the OLD replaced CASEY_DASHBOARD_TOKEN design (src/dashboard/server.js:16, src/dashboard/auth.js:2) -- zero live code paths reference these. Live HTTP witness against running casey instance on localhost:4000 (real process, PID 35920, confirmed via netstat LISTENING): GET /api/cases with Authorization: Bearer faketoken123 -> HTTP 401 (bearer ignored); GET /api/cases?token=faketoken123 -> HTTP 401 (query token ignored); GET /api/cases with no auth -> HTTP 401; GET /api/accounts (admin-only) with no auth -> HTTP 401; GET /api/cases with tampered cookie -> HTTP 401. Only the HttpOnly casey_session cookie, verified via verifySession + live account epoch check in routes/auth.js''s session middleware, ever populates req.caseyAccount.'
- session_id: casey-authsec-sub-7f3a91
  id: dashboard-admin-role-live-db-read
  obligation_kind: ownership
  prd_id: dashboard-security-auth-hardening
  description: Admin-only routes (accounts.js x6, contacts.js x1) check req.caseyAccount.role which is populated only from a fresh getAccount() DB row fetch per-request in the session middleware, never from the session cookie payload itself (cookie only carries id/exp/epoch, no role field).
  status: witnessed
  witness_evidence: 'src/dashboard/routes/auth.js lines 29-50: session middleware runs on every request, calls getAccount(store, claim.id) (a live thatcher DB fetch) and only sets req.caseyAccount if the fresh row exists, is not disabled, and claim.epoch matches the row''s LIVE session_epoch. src/dashboard/auth.js issueSession() (lines 70-74) encodes only {id, exp, epoch} into the signed cookie payload -- no role field ever leaves the server or round-trips through the cookie. server.js:129 isAdmin(req) reads req.caseyAccount?.role, which is only ever the freshly-fetched DB row''s role. Grep confirmed all 7 admin-only route checks (accounts.js x6, contacts.js x1) call isAdmin(req). Live HTTP witness: GET /api/accounts (admin-only) with no auth -> 401 (fails closed before role check reached).'
- session_id: 1787306301-0
  id: harness-prompt-architecture-witness
  obligation_kind: totality
  subject: caseSystemPrompt role-guardrail logging + full-extraction mandate + case_new not-trapped language
  description: 'Sub-session hardened src/hooks/prompt.js: added audit-trail logging instruction for detected role-rewrite attempts via case_report notes field (case_observe unreachable at reporter tier), strengthened full-extraction mandate (call case_report with EVERY new field each turn), strengthened case_new not-trapped guidance. Live witness via node ESM import: 7/7 edge-case inputs (null report, corrupted JSON, empty events, null contact) returned defined non-empty string; byte-identical output on replay (determinism); no input mutation (effect boundary); 12/12 required-phrase checks passed.'
  status: witnessed
  witness_evidence: 'node ESM live invocation of caseSystemPrompt (src/hooks/prompt.js) post-fix: 7/7 boundary inputs (null report, corrupted JSON, empty events array, null contact, wrong-type fields, max-size report, boundary-adjacent-invalid) returned defined non-empty string (totality). Two consecutive invocations with identical input produced byte-identical output (replay/determinism, no Math.random/Date.now in prompt path). Input object reference-compared before/after: no mutation (effect boundary, pure function confirmed). grep-verified: role-guardrail instruction present and now paired with an explicit case_report notes-field logging instruction (closes prior gap where role-rewrite attempts were ignored but not audited); full-extraction mandate now states ''call case_report with EVERY such field this turn -- never hold one back''; case_new guidance explicitly states old report data never traps a genuinely new report. node --check clean, ASCII-only confirmed, npm run lint passes all 136 files including pii-safety/pure-agent/no-stub-mock gates. Committed as lanmower, commit d32b1f0, not pushed.'
- session_id: casey-provsub-2216
  id: mut-provider-chain-queue-drain
  obligation_kind: postcondition
  subject: makeResilientCallLLM (src/llm.js) correctly throws on provider-down (enabling the LLM-down queue gate in hooks/handler.js), flips to ok:true and fires onRecover exactly once on the down->up rising edge (driving casey.js drainQueuedTurns via both bin/worker.js and the --no-supervise path in bin/casey-cli.mjs), delivers real replies post-recovery, and its resolve-debounce genuinely bounds re-probe frequency.
  description: Verify the provider down/recover/drain contract in llm.js's makeResilientCallLLM is real, not aspirational prose.
  status: witnessed
  witness_evidence: 'Live exec_js-equivalent witness (C:/Users/user/AppData/Local/Temp/claude/C--dev-casey/8d1d5f9f-adf8-43c8-9d58-1105c5fc7b05/scratchpad/witness-llm.mjs, run via `node witness-llm.mjs`) against the REAL src/llm.js module (import file:///C:/dev/casey/src/llm.js, real function substitution via the module''s own documented resolve/now injection seam, no mock framework): (1) provider.down() -> brain.callLLM() throws ''AI helper offline (provider unreachable); no reply sent'' -- confirmed this lets hooks/handler.js''s LLM-down queue gate append QUEUED-FOR-AGENT and return without blocking the contact. (2) status().ok===false while down, feeding the queue gate''s llmStatus() check. (3) provider.recovered() -> next status()/ensure() call resolves live, status flips to {source:acptoapi,ok:true}, and onRecover fires EXACTLY ONCE on the down->up rising edge (recovered===1, not fired again on subsequent up-state calls) -- this is the exact edge both bin/worker.js (line 125) and bin/casey-cli.mjs''s --no-supervise path (line 427) wire to casey.drainQueuedTurns(), confirmed by direct code read. (4) A real (non-fabricated) reply flows through brain.callLLM() post-recovery: witness printed ''real reply for: [...]'', proving the queued-turn drain path delivers genuine content, not silence. (5) resolveDebounceMs genuinely bounds re-probe frequency (resolveCalls stayed at 2 total across the down/up/down-again/status sequence, not one call per status() invocation) -- confirmed via the RESOLVE_DEBOUNCE_MS logic (llm.js lines 182-213). Separately confirmed via full-file reads of src/casey.js (lines 480-1005): startDrainPoll (523-540) reads CASEY_DRAIN_POLL_INTERVAL_MS-configurable interval and calls drainQueuedTurns on a timer independent of new inbound (matches the PRD''s explicit requirement); drainQueuedTurns (838-977) has a hard status()-gate at entry (never burns a queued message against a still-down provider), processes ALL queued msgIds oldest-first, marks queue-drive-attempted only AFTER a successful drive, and dead-letters via queue-drive-failed after retryCap=5 (matches ''queue never accumulates indefinitely'' via bounded retry, though the PRD''s CASEY_RESUME_MAX_AGE_MS age-based expiry applies specifically to resumePendingTurns''s boot-time sweep at line 667, not drainQueuedTurns, which is retry-count-bounded instead -- both are real bounded mechanisms, applied to their respective code paths as designed). resumePendingTurns (581-820) reads CASEY_RESUME_MAX_REDRIVES/CASEY_RESUME_SPACING_MS/CASEY_RESUME_MAX_AGE_MS (all confirmed read from process.env with documented defaults), applies RESUME_DEGRADED_RETRY_CAP=5 AND RESUME_MAX_AGE_MS ceiling, and case-level dead-letters (resume-exhausted tag) once every pending msgId is capped -- confirmed this satisfies ''no resume tries same failed config twice (learns and diverges)'' since acptoapi''s own availability.score() (node_modules/acptoapi/lib/auto-chain.js lines 516-556) is a live, real-time-updated observed-success/failure signal shared across all in-process calls, so a retry after a real failure re-ranks the candidate chain and genuinely routes around the model that just failed -- confirmed by direct source read, not inference. hooks/handler.js''s own 3-attempt retry loop (MAX_TOOL_CHOICE_ATTEMPTS=3, line 773) is bounded by TURN_HARD_DEADLINE_MS via a real remainingMs calculation per attempt (lines 838-844), and the terminal degraded-turn fallback (STILL_WORKING_TEXT / TURN_TIMEOUT_TEXT, lines 92-93) is confirmed via AGENTS.md line 395-399 to be the ONE deliberate, scoped, documented exception to the no-hardcoded-fallback-text rule (truthful about the WAIT, never fabricates understanding of the report) -- not a gap, matches design intent exactly.'
- session_id: casey-provsub-2216
  id: mut-timeout-coordination-drift
  obligation_kind: invariant
  subject: AGENTS.md's Timeout Coordination table documented ACPTOAPI_CHAIN_LINK_TIMEOUT_MS default as 20000ms; the actually-installed acptoapi package (floating github:AnEntrypoint/acptoapi#main, no version pin) ships DEFAULT_LINK_TIMEOUT_MS=120000ms in chain-machine.js -- a real, confirmed drift that broke the documented per-attempt >= per-link coordination invariant. casey's own .env already mitigates via an explicit ACPTOAPI_CHAIN_LINK_TIMEOUT_MS=60000 override, but AGENTS.md's prose and hooks/handler.js's inline comments both still claimed the stale 20s figure as load-bearing reasoning, and no doctor preflight caught the drift.
  description: Confirm and fix the stale 20s-vs-real-120s per-link timeout documentation drift, and add a doctor check that reads the live value instead of trusting prose.
  status: witnessed
  witness_evidence: 'Confirmed via direct source read: node_modules/acptoapi/lib/chain-machine.js line 6 -- `const DEFAULT_LINK_TIMEOUT_MS = Number(process.env.ACPTOAPI_CHAIN_LINK_TIMEOUT_MS) || 120000;` -- vs AGENTS.md''s (pre-fix) documented ''20000 / 20 sec'' default and hooks/handler.js''s matching stale inline comments (2 locations). Confirmed package.json''s acptoapi dependency spec is the floating `github:AnEntrypoint/acptoapi#main` (no version pin, line 22) -- the pre-condition stated in the PRD row. Confirmed casey''s own .env already sets ACPTOAPI_CHAIN_LINK_TIMEOUT_MS=60000 as an explicit mitigating override (grep .env line 7), meaning the LIVE running system was never actually exposed to the full 120s-per-hop risk, but a fresh deployment relying on AGENTS.md''s stale prose (or omitting the .env line) would silently inherit the wider 120s default with zero warning. FIXED: (1) AGENTS.md''s Timeout Coordination section (Layer 2 + coordination-rules code block) rewritten to state the real 120s upstream default, explain the drift, and reference the new doctor check. (2) hooks/handler.js''s two stale ''20s per-hop'' inline comments (near TURN_HARD_DEADLINE_MS''s declaration and near the attempt-loop''s remainingMs comment) corrected to state the real default and the .env override reason. (3) Added a new doctor preflight check in bin/casey-cli.mjs (after the scan-deps block) that reads the LIVE resolved ACPTOAPI_CHAIN_LINK_TIMEOUT_MS value -- explicit env var first, else parsed directly out of the installed node_modules/acptoapi/lib/chain-machine.js source via regex (not a hardcoded re-guess, so it self-corrects if acptoapi''s default drifts again) -- and flags `bad` if linkMs >= min(attemptMs,hardDeadlineMs) (a single hop can exhaust the whole budget) or `warn` if linkMs*2 >= budget (room for only 1 unhealthy hop). LIVE WITNESS: ran `node bin/casey.js doctor` to completion (captures to C:/Users/user/AppData/Local/Temp/claude/C--dev-casey/8d1d5f9f-adf8-43c8-9d58-1105c5fc7b05/scratchpad/doctor.out, exit code 0) -- output line 9 confirms: ''! timeout coordination: ACPTOAPI_CHAIN_LINK_TIMEOUT_MS=60000ms (ACPTOAPI_CHAIN_LINK_TIMEOUT_MS env) leaves room for at most 1 unhealthy hop within the 120000ms per-attempt budget - a chain walking 2+ bad providers in one attempt will exhaust it'' -- correctly reads the real .env-overridden value (not the raw acptoapi default) and correctly computes the warn-tier verdict against the real 120000ms budget (min of CASEY_LLM_TURN_TIMEOUT_MS default 120000 and CASEY_TURN_HARD_DEADLINE_MS default 120000). Separately ran node --check on both edited files (bin/casey-cli.mjs, src/hooks/handler.js) -- both pass with no syntax errors. Separately verified in isolation (node -e with no .env loaded) that when ACPTOAPI_CHAIN_LINK_TIMEOUT_MS is unset, the check correctly falls through to parsing acptoapi''s own installed 120000 default and correctly flags linkMs>=budget as true -- confirming the check would have caught this exact drift on a deployment missing the .env override, which is the entire point of the fix.'
- session_id: casey-obs-sub-4471
  id: obs-degraded-turn-write-read-contract
  obligation_kind: totality
  description: recordDegradedTurn (degraded-turns.js) must write an event that /api/turns/degraded, queryDegradedTurns, and calculateDegradationRate can all actually read back. Prior implementation wrote kind:'degraded_turn' with a pre-stringified data payload (double-JSON-encoded through case-store.js's own appendEvent JSON.stringify), invisible to every reader. Must converge on the single kind:'observation'+data.degraded_turn===true convention already used by hooks/handler.js's 4 direct appendEvent call sites.
  status: witnessed
  witness_evidence: 'Fixed src/degraded-turns.js recordDegradedTurn: kind changed from ''degraded_turn'' to ''observation'', data now a plain object with degraded_turn:true (was JSON.stringify''d, double-encoding through case-store.js''s own appendEvent stringify). Live witness (node script against a store double replicating case-store.js''s real appendEvent contract): pre-fix, evData(event) on the written event returned a STRING not an object (typeof ''string''), d.reason===undefined, d.degraded_turn===undefined, and the real /api/turns/degraded query shape found 0 of the recorded events. Post-fix, same witness: evData returns a real object, d.reason===''timeout'' correctly, and a live createDashboard() HTTP server (real thatcher-backed CaseStore, real auth login+forced-password-change flow, real case created via findOrCreateCase) returned GET /api/turns/degraded with count:2 and both reasons (timeout, provider) correctly surfaced -- ALL LIVE ASSERTIONS PASS (9/9) in the full HTTP witness.'
- session_id: casey-obs-sub-4471
  id: obs-provider-health-dead-branch
  obligation_kind: effect-boundary
  description: GET /api/health/provider's primary branch checked for a ProviderHealthTracker shape (s.status/s.lastSuccessAt/s.queuedTurnCount/s.currentChainPosition) that makeResilientCallLLM().status (the real llmStatus wired in bin/worker.js and bin/casey-cli.mjs) never produces -- permanently dead code masquerading as detail. Rewired to use real llmStatus() fields (source/model/degraded/lastMs/recentSlow) plus real queueStatus() (pending/deadLettered/truncated), matching the fields the live backend actually populates.
  status: witnessed
  witness_evidence: 'Rewired GET /api/health/provider (operations.js) to build its response from llmStatus()''s real fields (source/model/degraded/lastMs/recentSlow -- the actual makeResilientCallLLM().status shape, verified by reading llm.js''s status() implementation) and queueStatus()''s real fields (pending/deadLettered/truncated -- verified by reading casey.js''s queueStatus() implementation), removing the dead ProviderHealthTracker branch that checked for s.status/s.lastSuccessAt/s.queuedTurnCount/s.currentChainPosition, fields no real production wiring (bin/worker.js, bin/casey-cli.mjs both pass llmStatus:brainResilient.status or equivalent) ever populates. Live HTTP witness: injected a deterministic llmStatus function returning {source:''acptoapi'',model:''claude/sonnet'',degraded:true,lastMs:22000,recentSlow:2} and queueStatus returning {pending:3,deadLettered:1,truncated:false}; GET /api/health/provider correctly returned status:''degraded'', model:''claude/sonnet'', queued_turn_count:3, dead_lettered_count:1 -- all 4 related assertions passed against the real route handler over real HTTP.'
- session_id: casey-obs-sub-4471
  id: obs-cross-case-query-correctness
  obligation_kind: totality
  description: queryDegradedTurns/calculateDegradationRate called store.listEvents(null,...) believing it meant 'all cases' -- listEvents(caseId,...) is scoped to one case_id via a where-clause, so caseId=null matches nothing. Must use store.listAllEvents(where,...), the real cross-case query already used by the working /api/turns/degraded route.
  status: witnessed
  witness_evidence: 'Fixed queryDegradedTurns and calculateDegradationRate (degraded-turns.js) to call store.listAllEvents({kind,actor},{limit}) instead of store.listEvents(null,{limit}). Live witness: a store double implementing listEvents(caseId,opts) with the real case-store.js semantics (filters by case_id) showed listEvents(null,{limit:50000}) returns 0 rows even when 3 real events exist across 2 cases -- proving null was never treated as ''all cases''. Post-fix, listAllEvents-based queryDegradedTurns correctly returned summary.total:3 with by_reason:{timeout:1,provider:1,''retry-exhausted'':1} in the same witness run. Confirmed again in the full live HTTP server witness (real thatcher CaseStore): GET /api/turns/degraded and /api/health''s degradation_rate both reflect the 2 real recorded degraded turns.'
- session_id: casey-obs-sub-4471
  id: obs-timestamp-seconds-vs-ms
  obligation_kind: totality
  description: degraded-turns.js used new Date(event.created_at).getTime() on thatcher's unix-SECONDS created_at convention (case-store.js appendEvent), misreading every timestamp by 1000x. Must use the shared tsMs() digit-string-aware helper (timestamp.js) matching attn.js/case-health.js/case-sweep.js's own established convention.
  status: witnessed
  witness_evidence: 'Replaced new Date(event.created_at).getTime() with tsMs(event.created_at) (imported from timestamp.js, the shared digit-string-aware parser already used by attn.js/case-health.js/case-sweep.js) in both queryDegradedTurns and calculateDegradationRate. Live HTTP witness: real thatcher-created events (created_at stamped as unix-seconds integer per case-store.js''s own appendEvent, e.g. 1787316712) were correctly windowed by GET /api/turns/degraded''s since= filter and surfaced with count:2, proving the seconds-vs-ms conversion is now correct against real thatcher-stamped rows, not a synthetic ISO string.'
- session_id: casey-obs-sub-4471
  id: obs-health-cases-per-operator
  obligation_kind: totality
  description: PRD post-condition explicitly requires GET /api/health/cases returns case-level health signals PER OPERATOR; prior implementation returned a flat case list with no assignee/operator attribution or rollup. Added assignee field per case and a by_operator array (operator, case_count, breach_count) sorted worst-first.
  status: witnessed
  witness_evidence: 'Added assignee field to each case row and a by_operator rollup array (operator, case_count, breach_count, sorted worst-first) to GET /api/health/cases (operations.js), satisfying the PRD''s explicit post-condition ''GET /api/health/cases returns case-level health signals per operator''. Live HTTP witness: real case created and assigned via store.updateCaseQuiet(id,{assignee:''admin''}); GET /api/health/cases response included by_operator:[] (correctly empty since the test case had no active health breach) and every case row in the cases array carries the assignee field (assertion ''cases[].assignee field present'' passed). Field is an operator username, never PII (no external_id/contact_id), consistent with /api/attention''s existing assignee exposure.'
- session_id: casey-prov-sub-7714
  id: data-provenance-writeobservation-rank-tracking
  obligation_kind: totality
  description: writeObservation latestByField must correctly track the most-recent observation per field so canReplace rank-enforcement compares against the true current top, not a stale/first-ever value.
  prd_id: data-provenance-system
  status: witnessed
  witness_evidence: 'Live exec_js against real src/core/{provenance,observation,raw-log,write-path,pack-schema}.js and src/packs/{animal-health,water-point}.js (no mocks, no test files): 1) All 25 PROVENANCE_KINDS rank-pair combinations for canReplace() verified against expected rank ordering (unknown<inferred<reported<observed<measured) -- 0 mismatches. 2) Discovered real bug: writeObservation latestByField stored bare provenanced values (which carry recordedAt, not reportedAt); Date.parse(existing.reportedAt) was always NaN after the first write to a field, and  is always false in JS, so the rank-check baseline froze at the FIRST observation ever written for a field. Reproduced live: a 3rd write (inferred) after a 2nd write (measured) was silently ACCEPTED instead of rejected -- confirmed via direct exec_js reproduction before the fix (w3.rejectedFields=[] and location key present in accepted findings, when it should have been rejected). 3) Fixed in src/core/write-path.js (commit 4e4ea19, lanmower): latestByField now stores {val, reportedAt} wrapper objects, mirroring the already-correct pattern already used in redactSubjectFields in the same file. 4) Post-fix regression witness: same 3-write scenario now correctly rejects the 3rd (inferred) write, reports rejectedFields=[{field:location,reason:...}], and the accepted findings do NOT contain the clobbering value; final latest-by-field truth for location resolves to the LAST same-or-higher-rank write (measured w4, lat -26.0), not the first-ever value. 5) writeObservation genuinely rejects (never silently drops): rejectedFields array populated with field+reason+current+incoming on every rank violation; accepted findings never include a rejected field. 6) raw-log.js RawLog.prototype has NO update/delete/remove/set/edit/overwrite/mutate/patch method -- append() is the only write method, confirmed via Object.getOwnPropertyNames. 7) pack-schema.js validatePack() rejects unknownAllowed:false with an explicit error naming the offending field; loadPack() throws on such a pack. Confirmed live against a constructed bad pack. 8) Real packs (animal-health.js, water-point.js) both validate cleanly (valid:true, errors:[]) through the identical unmodified engine -- domain-neutrality confirmed. 9) requireProvenance/isProvenanced reject a bare object literal shaped like a provenanced value (no __provenanced marker) -- confirmed live. 10) mkValue enforces unknown<->null coupling both directions (unknown+non-null value throws; non-unknown+null value throws) -- confirmed live. 11) 10 concurrent writeObservation calls to the same subject via Promise.all serialize correctly through the per-subject async lock -- all 10 landed, 0 corrupt lines. 12) redactSubjectFields correctly bypasses rank check for erasure (a measured value is redactable by a reported-rank erasure), and is idempotent (second call against already-redacted field appends nothing). 13) End-to-end: real provenance-wire.js recordProvenanceObservation() caller path (the actual case_report integration) exercised post-fix with 3 sequential calls -- correct accept/reject behavior. 14) npm run lint passes clean (136 files, ascii+config+package+pure-agent+no-stub-mock+pii-safety) on the committed fix. Commit: 4e4ea19 fix(provenance): correct rank-tracking bug..., authored/committed as lanmower only, no AI attribution.'
- session_id: casey-supply-sub-1787318017
  obligation_kind: effect-boundary
  description: 'scan_deps full scan surfaced failCount=2, both hits are flatspace.config.mjs matching HiddenSpawn-class obfuscation signature (3247 byte/line ratio, _0x-hex identifiers, http+eval+detached-spawn payload). Investigated: deps/design (top-level) is clean at origin/main tip c4f961e, 196 bytes, no obfuscation. deps/freddie/design (NESTED submodule inside freddie, AnEntrypoint/design repo) is compromised: detached HEAD at 5aa2717, built on commit 68b2b255e88e28ff7e6de644bd6a678178759598 (2026-08-15, author lanmower) which injected the dropper. Upstream AnEntrypoint/design main was already fixed by commit 7cbfa9f2b1311c330a464693307a2e5b9d5749b9 (security: strip HiddenSpawn-class obfuscated payload, 2026-08-20) which IS on origin/main tip c4f961e. The local nested checkout at deps/freddie/design never advanced past the stale compromised lineage to pick up the fix -- it is detached, not tracking a branch. freddie itself also shows all 3 of its own nested submodules (acptoapi, design, plugsdk) as bare detached SHAs with no branch name in git submodule status, violating AGENTS.md submodule-management directive. This requires explicit user authorization before any reset/push -- report only, no fix applied.'
  depends_on: []
  id: mut-1787318719595
  status: witnessed
  witness_evidence: '{"scan_deps_result":{"failCount":1,"warnCount":136,"blockedCount":0,"nodeModulesTruncated":false,"filesScanned":12059,"failing":[{"path":"./deps/freddie/design/flatspace.config.mjs","ratio":3247,"note":"1682 _0x-hex obfuscator-style identifiers"}]},"root_cause":"deps/freddie/design (nested AnEntrypoint/design submodule inside freddie) detached at 5aa2717, built on compromised commit 68b2b255e88e28ff7e6de644bd6a678178759598 (2026-08-15, author lanmower). Upstream origin/main already fixed at 7cbfa9f2b1311c330a464693307a2e5b9d5749b9 (security: strip HiddenSpawn-class obfuscated payload, 2026-08-20), confirmed present in origin/main tip c4f961e793a3bef4ab3c682fbb6342bc79de1908.","runtime_exposure":"NOT reachable from casey runtime -- node_modules/freddie and node_modules/anentrypoint-design (the actual npm-resolved trees casey imports) do not contain flatspace.config.mjs at all; only the deps/ local-edit submodule checkout is affected, matching AGENTS.md architecture that deps/ is never imported at runtime. Real risk is limited to a developer running design/flatspace build tooling directly inside that submodule checkout.","top_level_deps_design_clean":true,"package_json_specs_confirmed":"acptoapi/anentrypoint-design/freddie/thatcher all github:owner/repo#main, no version pins","top_level_submodule_status":"deps/acptoapi, deps/design, deps/freddie all (heads/main); deps/thatcher shows describe-string v1.0.91-3-g9977155 but confirmed via git symbolic-ref HEAD = refs/heads/main, up to date with origin/main -- not detached, cosmetic describe-string only","freddie_own_nested_submodules_all_detached":"freddie git submodule status shows acptoapi(f8ab9eb), design(5aa2717), plugsdk(v1.0.26) all as bare detached SHA, no branch name -- violates AGENTS.md submodule-management directive for ALL THREE nested deps, not just the compromised one","action_taken":"NONE -- report only per gm Section 1a and explicit task instruction; requires user authorization before reset/push"}'
- session_id: 1787306301-0
  id: supply-chain-integrity-remediation
  obligation_kind: totality
  subject: Remediated live HiddenSpawn-class dropper found by casey-supply-sub-2216 in deps/freddie/design/flatspace.config.mjs (nested submodule detached at compromised commit 68b2b25)
  description: 'User-authorized fix: synced all 3 of freddie''s detached nested submodules (design, acptoapi, plugsdk) to their clean origin/main tips. design now at 08f0e3d (27 commits ahead of the compromised detached SHA, includes upstream security fix 7cbfa9f). flatspace.config.mjs confirmed 196 bytes, legitimate config content, no obfuscated payload. acptoapi now at 193ef56, plugsdk now at 5b1c3ed (genuinely on refs/heads/main per git symbolic-ref, describe-string is cosmetic). Casey''s own runtime was never exposed (node_modules/freddie and node_modules/anentrypoint-design never contained this file).'
  status: witnessed
  witness_evidence: 'scan_deps {"full":true} pre-fix: failCount=1 (deps/freddie/design/flatspace.config.mjs, ratio 3247, 1682 _0x-hex identifiers, HiddenSpawn signature). Root-caused to nested submodule detached at compromised commit 68b2b25 (2026-08-15, disguised as prose fix). Fixed upstream in AnEntrypoint/design main at 7cbfa9f (2026-08-20). Remediation: git checkout main && git reset --hard origin/main in deps/freddie/design (now 08f0e3d), deps/freddie/acptoapi (now 193ef56), deps/freddie/plugsdk (now 5b1c3ed, confirmed refs/heads/main via git symbolic-ref). flatspace.config.mjs confirmed 196 bytes clean config post-fix. scan_deps {"full":true} post-fix (dispatch verify-2): failCount=0, blockedCount=0, warnCount=136 (all pre-existing benign minified bundles, unchanged from pre-fix baseline). package.json confirmed all 4 composed deps use github:owner/repo#main spec, no version pins. Top-level deps/{acptoapi,design,freddie,thatcher} all confirmed on live main branch tips.'
- session_id: casey-statemachine-sub-7f21
  prd_id: case-lifecycle-machine-correctness
  obligation_kind: replay
  subject: case-machine.js (xstate v5 machine, built from thatcher.config.yml's case_lifecycle graph) is total (never throws on any state x event pair, including undefined/garbage events), every state x event x role combination through canTransition/nextStates never crashes, 3+ representative transitions are idempotent when replayed, and 3 confluence path-pairs (different event orderings to the same logical endpoint) produce equivalent resulting state.
  depends_on: []
  id: mut-1787320775993
  status: witnessed
  witness_evidence: 'Live exec_js witness (node ESM, real xstate v5.32.5, real buildCaseMachine/canTransition/nextStates from src/case-machine.js, real workflow graph transcribed from thatcher.config.yml''s case_lifecycle block: new{fwd:[triaging,in_progress]}, triaging{fwd:[in_progress,waiting,resolved],bwd:[new]}, in_progress{fwd:[waiting,resolved],bwd:[triaging]}, waiting{fwd:[in_progress,resolved]}, resolved{fwd:[closed],bwd:[in_progress]}, closed{requires_role:[operator,admin],bwd:[resolved]}). TOTALITY: createActor(machine,{snapshot:machine.resolveState({value,context})}) then .send() for all 6 states x 10 events (6 real GO_* events plus garbage: GO_NONEXISTENT, DELETE, empty string, GO_NEW, RANDOM_EVENT_123, GO_CLOSED-from-wrong-state) = 60 combinations, 0 crashes, 0 unexpected state drift on any unhandled event (xstate v5''s documented default: an event with no `on` handler in the current state is silently ignored, confirmed live not assumed). canTransition/nextStates exercised across every (from,to,role) triple including bogus_state/empty/undefined/null from-values and garbage roles = 326 combinations, 0 crashes -- both return {ok:false,error} shapes, never throw. IDEMPOTENCY: 6 representative transitions (new->triaging, triaging->in_progress, in_progress->waiting, resolved->closed, closed->resolved, in_progress->triaging) each sent twice in a row from the same actor; all 6 produced identical afterFirst===afterSecond state values. CONFLUENCE: 3 path-pairs to the same logical endpoint (new->waiting via triaging vs direct; new->closed via a full reopen/reclose cycle vs direct; new->resolved via a wandering multi-hop path vs direct) all produced value-and-context-equivalent final snapshots (machine carries no context field at all, confirmed by reading createMachine({id,initial,states}) has no context key). ADVERSARIAL role-gate check: closed->resolved (backward, reopen) correctly rejects reporter role and undefined/missing role (fail-closed), accepts operator/admin; resolved->closed (forward, close) correctly unrestricted for any role including undefined -- matches the documented directional role-gate design (a backward edge gates on the FROM node''s requires_role, a forward edge is never gated). No gap found in case-machine.js itself -- it is already total, confluent, and idempotent as designed.'
- session_id: casey-statemachine-sub-7f21
  prd_id: case-lifecycle-machine-correctness
  obligation_kind: effect-boundary
  subject: CaseStore.transition() (src/case-store.js) returns a case snapshot whose _version reflects EVERY mutation the call itself performed, including appendEvent's own touch write (last_event_at), not just the first of two writes -- so a caller reusing the returned _version as its next optimistic-lock expectedVersion never hits a spurious conflict against a write transition() itself already made.
  depends_on: []
  id: mut-1787320777999
  status: witnessed
  witness_evidence: 'Live witness against a REAL CaseStore + REAL thatcher (real better-sqlite3, cwd-bound per AGENTS.md, isolated scratch cwd, no mocks). Found and fixed a genuine replay/effect-boundary bug in src/case-store.js''s transition(): the pre-fix code captured `const result = await this.getCase(caseId)` BEFORE calling appendEvent(), but appendEvent''s own touch write (touch:true by default, line ~1418: this.t.update(''case'', caseId, {last_event_at:...})) is a SECOND mutation of the same case row that bumps thatcher''s optimistic-lock _version again -- so transition()''s returned snapshot was always exactly one version behind the row''s true post-call state. LIVE REPRODUCTION (pre-fix): created a real case (findOrCreateCase), called transition(id,''triaging''): returned _version=''1''; an immediate separate getCase() on the same case showed _version=''2'' -- objective proof the returned object was stale the instant the call returned. A downstream simulation (updateCaseQuiet with expectedVersion=stale t1._version, the exact pattern case-sweep.js/dashboard PATCH routes use) showed the staleness is a live replay-safety violation (thatcher''s own lock never let it corrupt data -- a mismatched expectedVersion always throws -- but it broke the guarantee that transition()''s return value reflects every mutation the call itself performed). FIX: moved `const result = await this.getCase(caseId)` to after appendEvent() completes. POST-FIX LIVE RE-WITNESS: t1._version now correctly reads ''2'', matching a separate immediate getCase() read (also ''2'') -- staleness eliminated. Full replay suite re-run: a genuine no-op replay (transition() called twice with the case already at the target state) correctly short-circuits at the `before.status===toState` early return (case-store.js line 1497) with zero writes and an unchanged _version (versionUnchangedOnReplay:true) -- true replay-safety, not an artifact of the touch-write staleness. Event-log check confirmed the no-op replay does not double-append a transition event (1 event total, not 2). CONFLUENCE UNDER REAL CONCURRENT CONFLICT: two real concurrent transition() calls (Promise.allSettled) racing the same case to the same target status=''closed'' both settled fulfilled and converged to the identical final state -- exercises the real conflict-retry path (lines 1519-1529: on a version conflict, re-read fresh, re-validate against CURRENT status not stale data, and treat ''someone else already made this exact move'' as success rather than erroring). npm run lint (node scripts/lint.mjs) passes clean: ''136 JS files syntax-checked, config + package + ascii + pure-agent + no-stub-mock + pii-safety clean''. All scratch/witness files removed after use; only src/case-store.js was modified (13-line diff: moved one statement, added an explanatory comment).'
- session_id: casey-perfbase-sub-8f31a2
  id: perfbase-live-http-timing-witness
  obligation_kind: replay
  depends_on: []
  description: 'Live HTTP timing measurements against running casey up instance (localhost:4000, real thatcher db.sqlite, 23 real cases): dashboard root, api_health_provider, api_cases_list, api_cases_detail, api_cases_update (PATCH), api_health_cases sweep-state read. Auth via real POST /api/login round trip against a throwaway operator_account inserted with the app''s own scrypt hashPassword scheme, cleaned up after measurement (DELETE, changes:1 confirmed). end_to_end_report and full sweepCases() write-path duration explicitly marked not_measurable/caveated with disclosed real reasons (no LLM credential configured; avoiding a second live sqlite handle), not silently omitted.'
  status: witnessed
  witness_evidence: 'Real live exec_js HTTP witnesses against localhost:4000 (casey up already running, pid on port 4000, real data/db.sqlite 122880 bytes, 23 cases). Results written to C:/dev/casey/.gm/performance-baseline-2026-08-21.json: dashboard_root authed mean 11.868ms max 17.141ms (target 2000ms LCP, pass); api_health_provider authed mean 74.14ms max 120.88ms (target 50ms, FAIL worst-case 141% over -- root cause: uncached multi-source live read, documented); api_cases_list authed mean 58.869ms max 301.103ms cold-start outlier then steady 20.7-46.8ms (target 100ms, cold-hit fails, steady-state passes); api_cases_detail (real route GET /api/cases/:id, corrected from assumed :ref) mean 11.817ms max 18.757ms (target 200ms, pass all 10 attempts); api_cases_update (real route PATCH /api/cases/:id, corrected from assumed POST .../update) mean 106.149ms max 267.257ms single-attempt outlier, 9/10 in-budget (target 200ms, one outlier fails, documented as not sustained); GET /api/health/cases sweep-state read mean 32.615ms max 54.721ms, real sweep data (scanned:23, errors:0, last_run_at real epoch ms). Auth obtained via genuine POST /api/login (200, real HMAC session cookie) against a throwaway operator_account inserted via node:sqlite direct write using the app''s own src/dashboard/auth.js hashPassword scrypt scheme (N:16384,r:8,p:1) -- not a forged/bypassed session. Account deleted after use (DELETE WHERE username LIKE ''perfbase-%'', changes:1, confirmed via follow-up exec_js). end_to_end_report and full sweepCases() scan-and-tag write-path duration left explicitly not_measurable with disclosed real reasons in the JSON (GET /api/health confirmed source:none, no LLM credential in this dev .env; avoiding a second live thatcher/sqlite store handle against the running instance per AGENTS.md''s documented Windows file-lock risk) -- never fabricated, matching the no-mocks invariant. Replay property: every series ran n=5 to n=10 live attempts per endpoint in the same turn, worst-case (max_ms) captured alongside mean, never average-only, satisfying the PRD''s worst-case-not-average-only invariant. JSON validated well-formed (node JSON.parse) and ASCII-only (0 non-ascii chars) before commit.'
- session_id: casey-uxpolish-sub-9f21-1
  id: dashboard-ux-polish-effect-boundary
  obligation_kind: effect-boundary
  prd_id: dashboard-ux-polish
  description: Every focus-visible/design-token/reduced-motion rule in src/dashboard/public CSS resolves through a real deps/design token (--accent, --focus-color, --shadow-3/--shadow-overlay), never a raw hex/px literal or a reference to a nonexistent token name; every clickable element with an onclick handler is keyboard-reachable (native button or explicit tabindex+role=button+onkeydown) and carries a :focus-visible outline rule.
  status: witnessed
  witness_evidence: 'Prior sessions (commits 196498c, 98a0264, dcaf881, 881814d, 9d14e1c) already closed the bulk of this PRD: token/spacing conversion, WCAG focus-visible baseline, reduced-motion guards, and dashboard-state-stability E2E verification. This session ran npm run lint (136 files clean) plus deps/design''s own lint-tokens.mjs/lint-inline-css.mjs/lint-inline-styles.mjs directly against src/dashboard/public (the real gate casey/scripts/lint.mjs wraps via DS_LINT_EXTRA_CSS_FILES/DS_LINT_EXTRA_JS_DIRS when deps/design is checked out) and found 3 real remaining gaps the prior passes missed: (1) dispatch-picker.css:23 .casey-dispatch-ok:focus-visible fell back to raw #fff instead of var(--accent), inconsistent with its 3 sibling focus-visible rules in the same file; (2) case-detail.css:80 .casey-dialog-card referenced var(--shadow-lg, ...) where --shadow-lg does not exist anywhere in deps/design (confirmed via grep across deps/design/*.css), so the raw rgba(0,0,0,.45) fallback was the only thing that ever rendered -- a real design-token bypass; (3) two real clickable elements had zero keyboard-accessibility coverage: .casey-meta-toggle (case-detail.css, a real <button>) had no :focus-visible outline, and .ds-handoff-banner (app.css/handoff-banner.js) was a bare <div onclick> with no tabindex/role -- unreachable by keyboard entirely, worse than a missing focus ring. Fixed all 3: dispatch-picker.css and case-detail.css now route through var(--accent)/var(--shadow-overlay, var(--shadow-3)); handoff-banner.js gained tabindex=0/role=button/aria-label/onkeydown(Enter,Space) matching the existing handover-panel.js/report-field.js clickable-div precedent; both elements gained :focus-visible outline rules. LIVE WITNESS against the real casey up instance already running on localhost:4000 (verified via curl 200 before touching it): opened a real CDP (Chrome) session via the cdp verb, navigated to http://localhost:4000/, confirmed api/whoami 200 (real authenticated session) and all dashboard CSS/JS served via 304 conditional-GET (supervisor hot-reload serving current code). Inspected the live document.styleSheets and confirmed all 3 fixed selectors'' exact new rule text was present in the loaded stylesheet. Injected a real <button class=casey-meta-toggle> and a real <div class=ds-handoff-banner tabindex=0 role=button> into the live DOM, called .focus() on each, and read getComputedStyle: both rendered outline ''solid rgb(92, 191, 82) 1.6px'' -- rgb(92,191,82) is the live --accent token (#5CBF52, confirmed via getComputedStyle(document.body).getPropertyValue(''--accent'')). document.activeElement.className after focusing the handoff banner div confirmed it is now reachable via keyboard focus (previously impossible, zero tabindex). Post-fix, re-ran npm run lint (136 files clean) and deps/design''s lint-tokens/lint-inline-css/lint-inline-styles directly (all PASS/OK, zero new violations, baseline ratchets unchanged or improved). Swept both remaining CSS files (app.css, case-detail.css) programmatically for every cursor:pointer selector missing a matching :focus-visible rule and every onclick div/span missing role=button+tabindex across the entire src/dashboard/public/src JS tree -- zero remaining gaps found after the fix. All transition/animation declarations in all 3 CSS files (app.css, case-detail.css, dispatch-picker.css) confirmed covered by an explicit @media (prefers-reduced-motion: reduce) block, verified selector-by-selector, not just presence-of-media-query. Committed (content verified present in HEAD via git show) -- author lanmower, no AI attribution, npm run lint passing pre-commit.'
- session_id: casey-edgecases-sub-7f3d21
  prd_id: edge-cases-comprehensive
  obligation_kind: totality
  subject: 'Burst messaging: per-contact in-flight guard buffers a rapid message burst and replays each buffered message via a self-perpetuating drain chain (handler.js''s trailing handleInbound block), never silently dropping any.'
  description: 'Live witness: 3 concurrent handleInbound calls for the same conversationKey against a real makeCaseHandler+store double.'
  witness_evidence: 'node live exec against file:///C:/dev/casey/src/hooks/handler.js''s real makeCaseHandler: 3 concurrent calls (msg1/msg2/msg3, same external_id BC:burstuser) via Promise.all. msg1 processed synchronously (inFlight claimed atomically pre-await); msg2/msg3 hit inFlight.has()===true, buffered into pendingBuffer (buffered:true,skipped:true returned immediately). Post-settle (300ms), recordInbound was called exactly 3 times with distinct texts [''msg1'',''msg2'',''msg3''] in that order -- proving the trailing drain block (handler.js line ~1400-1420, this.handleInbound(platform,next) fired after each turn completes) recursively replayed msg2 then msg3, each getting its own real inbound event. No message lost; no duplicate. Matches AGENTS.md''s burst-buffer-and-replay design principle exactly. No code change needed -- mechanism already correct.'
  id: mut-1787321445716
  status: witnessed
- session_id: casey-edgecases-sub-7f3d21
  prd_id: edge-cases-comprehensive
  obligation_kind: postcondition
  subject: 'Provider-down-then-recovery queue drain: real call sites wire makeResilientCallLLM''s onRecover edge to casey.drainQueuedTurns(), independently corroborating sibling session casey-provsub-2216''s already-fixed/verified llm.js recovery logic.'
  description: Re-confirm (not re-audit) that the queue-drain call site exists in the real flow, per task instructions.
  witness_evidence: 'grep-confirmed 2 real call sites: bin/worker.js:125 wires makeResilientCallLLM''s onRecover callback directly to caseyRef.drainQueuedTurns().catch(...); casey.js:531 runs an independent periodic drain-poll (startDrainPoll, CASEY_DRAIN_POLL_INTERVAL_MS-gated) that also calls this.drainQueuedTurns() so a queued contact is not solely dependent on a new inbound arriving to trigger drain. This matches mutable mut-provider-chain-queue-drain (session casey-provsub-2216, status witnessed) already in .gm/mutables.yml, which live-witnessed the down/recover/drain/deliver cycle against the real llm.js module. No further fix needed.'
  id: mut-1787321445824
  status: witnessed
- session_id: casey-edgecases-sub-7f3d21
  prd_id: edge-cases-comprehensive
  obligation_kind: invariant
  subject: conversationKey genuinely produces distinct per-author keys within one multi-author Discord channel (container:author), while replyTarget correctly stays the shared channel for delivery -- two workers in the same channel get separate cases, never collide.
  description: Live witness with two distinct real-shaped author ids in the same channel_id.
  witness_evidence: 'node live exec against file:///C:/dev/casey/src/hooks/handler.js''s real conversationKey/replyTarget exports: msgA={raw:{channel_id:CHAN123},from:USER_ALICE}, msgB={raw:{channel_id:CHAN123},from:USER_BOB} -- conversationKey(msgA)===''CHAN123:USER_ALICE'', conversationKey(msgB)===''CHAN123:USER_BOB'' (distinct, both contain the shared channel prefix). replyTarget(msgA)===replyTarget(msgB)===''CHAN123'' (both deliver to the same channel, correct per Discord''s channel-not-author POST target). A 1:1 WhatsApp-shaped message (no separate container) correctly collapses to the bare author id (''+27821234567''), confirming the container!==author branch condition in conversationKey (handler.js line 1436) is exercised correctly both ways. No code change needed -- mechanism already correct.'
  id: mut-1787321445806
  status: witnessed
- session_id: casey-edgecases-sub-7f3d21
  prd_id: edge-cases-comprehensive
  obligation_kind: effect-boundary
  subject: 'field_worker promotion mid-case: tier is resolved fresh from store.getContact() on every single turn invocation and recomputed inside the per-attempt retry loop, never cached/memoized across turns.'
  description: Live witness that a tier flip between two sequential reads is reflected on the second read with no caching layer.
  witness_evidence: 'node live exec: constructed a store double whose getContact(id) returns a mutable currentTier variable. Called store.getContact once (tier=reporter, resolvedTier=reporter matching handler.js''s tier-ternary expression: contact tier field_worker check falls back to reporter), flipped currentTier to field_worker (simulating an operator promotion), called store.getContact again -- resolvedTier immediately reflects field_worker. Source-confirmed handler.js: the contact variable is read via store.getContact(fresh.contact_id) unconditionally at the top of every handleInboundOnceClaimed call (no module-level or per-contact cache variable), and resolvedTier (around line 857) is recomputed inside the per-ATTEMPT retry loop from that same fresh contact object, so even a retried attempt within one turn re-derives tier from the same fresh read. No code change needed -- mechanism already correct, no restart required for a promotion to take effect.'
  id: mut-1787321458131
  status: witnessed
- session_id: casey-edgecases-sub-7f3d21
  prd_id: edge-cases-comprehensive
  obligation_kind: postcondition
  subject: 'Provider-down-then-recovery queue drain: real call sites wire makeResilientCallLLM''s onRecover edge to casey.drainQueuedTurns(), independently corroborating sibling session casey-provsub-2216''s already-fixed/verified llm.js recovery logic.'
  description: Re-confirm (not re-audit) that the queue-drain call site exists in the real flow, per task instructions.
  witness_evidence: 'grep-confirmed 2 real call sites: bin/worker.js line 125 wires makeResilientCallLLM''s onRecover callback directly to caseyRef.drainQueuedTurns().catch(...); casey.js line 531 runs an independent periodic drain-poll (startDrainPoll, CASEY_DRAIN_POLL_INTERVAL_MS-gated) that also calls this.drainQueuedTurns() so a queued contact is not solely dependent on a new inbound arriving to trigger drain. This matches mutable mut-provider-chain-queue-drain (session casey-provsub-2216, status witnessed) already in .gm/mutables.yml, which live-witnessed the down/recover/drain/deliver cycle against the real llm.js module. No further fix needed.'
  id: mut-1787321570727
  status: witnessed
- session_id: casey-edgecases-sub-7f3d21
  prd_id: edge-cases-comprehensive
  obligation_kind: postcondition
  subject: case_new is a real reachable tool at every access tier (REPORT_ONLY_TOOLS), never gated behind field_worker tier, and there is no deterministic override forcing case reuse -- resuming an old case vs starting fresh is entirely the agent's own call via case_new/case_switch.
  description: Static confirmation via case-tools.js source read (case_new's own gate exemption + gateByTier's fail-closed logic).
  witness_evidence: 'Read case-tools.js in full: REPORT_ONLY_TOOLS = new Set of case_report, case_stop, case_handoff, case_new (line 862) is explicitly documented as available at every tier, including the default reporter tier -- comment states verbatim that case_new (opening a fresh case for a genuinely new situation) applies the never-a-dead-end design principle to every reporter, not only field workers. gateByTier(tool) (line 886) only wraps tools NOT in REPORT_ONLY_TOOLS with the field_worker-tier check; case_new returns unwrapped (line 887: if REPORT_ONLY_TOOLS.has(tool.name) return tool), so it always reaches the handler regardless of ctx.tier. case_new''s own handler (lines 639-666) has no deterministic conflict/reuse-forcing logic -- it unconditionally creates a new case via store.createCase and rebinds the turn''s active case (rebindActiveCase), matching AGENTS.md''s stated design that there is no deterministic conflict detector, field-merge is fill-if-empty, and a contact who has clearly moved on is entirely the agent''s judgment call. No code change needed -- mechanism already correct.'
  id: mut-1787321570782
  status: witnessed
- session_id: casey-edgecases-sub-7f3d21
  prd_id: edge-cases-comprehensive
  obligation_kind: postcondition
  subject: case_new is a real reachable tool at every access tier (REPORT_ONLY_TOOLS), never gated behind field_worker tier, and there is no deterministic override forcing case reuse -- resuming an old case vs starting fresh is entirely the agent's own call via case_new/case_switch.
  description: Static confirmation via case-tools.js source read (case_new's own gate exemption + gateByTier's fail-closed logic).
  witness_evidence: 'Read case-tools.js in full: REPORT_ONLY_TOOLS = new Set of case_report, case_stop, case_handoff, case_new (line 862) is explicitly documented as available at every tier, including the default reporter tier -- comment states verbatim that case_new (opening a fresh case for a genuinely new situation) applies the never-a-dead-end design principle to every reporter, not only field workers. gateByTier(tool) (line 886) only wraps tools NOT in REPORT_ONLY_TOOLS with the field_worker-tier check; case_new returns unwrapped (line 887: if REPORT_ONLY_TOOLS.has(tool.name) return tool), so it always reaches the handler regardless of ctx.tier. case_new''s own handler (lines 639-666) has no deterministic conflict/reuse-forcing logic -- it unconditionally creates a new case via store.createCase and rebinds the turn''s active case (rebindActiveCase), matching AGENTS.md''s stated design that there is no deterministic conflict detector, field-merge is fill-if-empty, and a contact who has clearly moved on is entirely the agent''s judgment call. No code change needed -- mechanism already correct.'
  id: mut-1787321570824
  status: witnessed
- session_id: casey-edgecases-sub-7f3d21
  prd_id: edge-cases-comprehensive
  obligation_kind: totality
  subject: 'Burst messaging: per-contact in-flight guard buffers a rapid message burst and replays each buffered message via a self-perpetuating drain chain (handler.js''s trailing handleInbound block), never silently dropping any.'
  description: 'Live witness: 3 concurrent handleInbound calls for the same conversationKey against a real makeCaseHandler+store double.'
  witness_evidence: 'node live exec against file:///C:/dev/casey/src/hooks/handler.js''s real makeCaseHandler: 3 concurrent calls (msg1/msg2/msg3, same external_id BC:burstuser) via Promise.all. msg1 processed synchronously (inFlight claimed atomically pre-await); msg2/msg3 hit inFlight.has()===true, buffered into pendingBuffer (buffered:true,skipped:true returned immediately). Post-settle (300ms), recordInbound was called exactly 3 times with distinct texts msg1,msg2,msg3 in that order -- proving the trailing drain block (handler.js around line 1400-1420, this.handleInbound(platform,next) fired after each turn completes) recursively replayed msg2 then msg3, each getting its own real inbound event. No message lost; no duplicate. Matches AGENTS.md''s burst-buffer-and-replay design principle exactly. No code change needed -- mechanism already correct.'
  id: mut-1787321570863
  status: witnessed
- session_id: casey-edgecases-sub-7f3d21
  prd_id: edge-cases-comprehensive
  obligation_kind: invariant
  subject: conversationKey genuinely produces distinct per-author keys within one multi-author Discord channel (container:author), while replyTarget correctly stays the shared channel for delivery -- two workers in the same channel get separate cases, never collide.
  description: Live witness with two distinct real-shaped author ids in the same channel_id.
  witness_evidence: 'node live exec against file:///C:/dev/casey/src/hooks/handler.js''s real conversationKey/replyTarget exports: msgA has raw.channel_id=CHAN123, from=USER_ALICE; msgB has raw.channel_id=CHAN123, from=USER_BOB. conversationKey(msgA) equals CHAN123:USER_ALICE, conversationKey(msgB) equals CHAN123:USER_BOB (distinct, both contain the shared channel prefix). replyTarget(msgA) equals replyTarget(msgB) equals CHAN123 (both deliver to the same channel, correct per Discord''s channel-not-author POST target). A 1:1 WhatsApp-shaped message (no separate container) correctly collapses to the bare author id, confirming the container!==author branch condition in conversationKey (handler.js line 1436) is exercised correctly both ways. No code change needed -- mechanism already correct.'
  id: mut-1787321571073
  status: witnessed
- session_id: casey-perfgate-sub-8f2a
  prd_id: performance-gates-measurement
  obligation_kind: postcondition
  subject: 'GET /api/health/provider and GET /api/health worst-case latency regression (baseline: mean 74.14ms/max 120.88ms against 50ms target, root-caused to queueStatus() N+1 store round trips) fixed via single cross-case query; PATCH /api/cases/:id 267ms outlier re-verified at n=30 as non-sustained (no fix, per no-speculative-optimization invariant); LCP/case-list/map-render targets live-verified via real browser Resource/Navigation Timing.'
  description: Live before/after timing witness for queueStatus() N+1 fix (src/casey.js, src/case-store.js), PATCH outlier re-verification, and browser-witnessed LCP/case-list/map-render targets from the PRD description.
  witness_evidence: 'ROOT CAUSE CONFIRMED: casey.js queueStatus() (called by both GET /api/health and GET /api/health/provider on every poll) did store.listCases({limit:200}) then looped store.listEvents(c.id) once PER case -- up to 1+200 sequential store round trips per single API call. FIX: added caseIds param to case-store.js listAllEvents() (thatcher case_id $in operator-where, same compiler listCases already calls directly per its own comment), rewrote queueStatus() to fetch all relevant events cross-case in ONE query, group by case_id in JS, then apply the IDENTICAL per-case in-order marker scan (QUEUED-FOR-AGENT/queue-drive-failed/outbound-or-draft-completes) as before -- same live-read semantics (no caching added), same classification logic, fewer queries. LIVE BEFORE/AFTER WITNESS (git stash to get true pre-fix code running under the live casey up instance on localhost:4000, real throwaway admin operator_account via real POST /api/login, cleaned up after each measurement): BEFORE (original N+1 code, git-stashed): GET /api/health/provider n=15 mean=39.696ms max=72.736ms; GET /api/health n=10 mean=53.589ms max=85.383ms. AFTER (fix restored, git stash pop): GET /api/health/provider n=15 mean=11.318ms max=36.671ms (69% mean reduction, 50% worst-case reduction); GET /api/health n=10 mean=26.980ms max=35.114ms (50% mean reduction, 59% worst-case reduction). Functional correctness re-verified post-fix: queued_turn_count/dead_lettered_count/queue_truncated fields return identical correct values (0/0/false, matching real empty-queue state) -- fix changed query COUNT only, not the live-read contract (still zero caching, reads real event rows every call, per harness-observability-health''s never-cached-opinion invariant). PATCH /api/cases/:id: baseline''s single 267ms outlier (n=10) re-run at n=30 against a real case -- 0/30 exceeded 200ms target, mean=67.698ms p50=57.009ms p90=108.194ms max=143.683ms -- confirms the outlier was a genuine non-sustained one-off (transient contention/GC), not a systemic defect; no fix applied (a fix targeting an unreproduced single outlier would be speculative optimization, forbidden by this PRD row''s own invariant). BROWSER-WITNESSED TARGETS (live cdp session against localhost:4000, real authenticated session cookie, real 24-case dataset): dashboard root Navigation Timing domContentLoadedEventEnd=501.2ms loadEventEnd=514.3ms responseStart(TTFB)=11ms (target 2000ms LCP -- true LCP PerformanceObserver entry unavailable because the CDP-automated tab reports document.hidden=true/visibilityState=hidden during initial load per spec, same documented limitation as the prior baseline session; loadEventEnd is the real measured floor a browser-painted LCP sits above). GET /api/cases (case-list query) via real Resource Timing: 11.8ms and 12.6ms across repeat loads (target 100-200ms). GET /api/map/cases via real Resource Timing: 21.7-24.1ms; fresh-reload click-to-Leaflet-.leaflet-container-mounted: 94ms (target 500ms) -- all targets pass with wide margin. Lint clean post-fix: node scripts/lint.mjs -> ''136 JS files syntax-checked, config + package + ascii + pure-agent + no-stub-mock + pii-safety clean''. All throwaway operator_account rows created for auth (perfgate-*, perfverify-*, perfpatch-*, perfbrowse-*) confirmed deleted (SELECT username FROM operator_account WHERE username LIKE perf% returned empty array post-cleanup). SEPARATE FINDING (out of scope, not fixed): map-panel.js''s module-level `mounted` boolean singleton (line 22) failed to reliably remount Leaflet into a fresh .ds-map-canvas on repeat Map-panel navigation within one browser session during manual repro (canvas stayed empty, 0 children, no console errors) -- a real functional SPA bug, not a performance regression; recorded here for a future PRD row, not actioned in this measurement-scoped session.'
  id: mut-1787322000946
  status: witnessed
- id: opworkflow-assisted-mode-real-gate
  obligation_kind: effect-boundary
  prd_id: operator-workflow-stability
  subject: Assisted mode is a real send-blocking delivery gate enforced at the handler's early-return, not a UI label -- canAgentAct(caseRow,'reply')==='draft' short-circuits src/hooks/handler.js before the real adapter.send() call site (line ~1358) is ever reached; the only send path for a drafted reply is POST /api/cases/:id/draft/approve, gated by session auth + a real draft-pending tag check.
  status: witnessed
  witness_evidence: 'Live exec_js witness (node ESM, real src/hooks/heuristics.js canAgentAct, no mocks): 13/13 assertions passed. canAgentAct total and correct across all inputs (observe->none, assisted->draft, auto->send, missing/null/garbage autonomy all fail-open to send matching the auto default -- confirmed unreachable via any real write path since PATCH /api/cases/:id validates autonomy against an AUTONOMY enum before it ever reaches the store, src/dashboard/routes/cases.js:255). Simulated the real handler.js control-flow shape (mirroring the exact early-return at handler.js:1205-1222): when canAgentAct returns draft, the function returns {text:""} via a JS return statement BEFORE the real adapter.send() call site at line 1358 -- confirmed structurally unreachable in that invocation (send call count stayed 0 across the assisted-mode branch, vs exactly 1 in auto mode). Confirmed the ONLY send path for a drafted reply is POST /api/cases/:id/draft/approve (src/dashboard/routes/cases.js:775-802): requires authed(req) (401 without session), requires a real draft-pending tag via pendingDraft() (409 if none), and only then calls sendReply(c,text) -- read live from source, ordering confirmed auth-gate then draft-existence-gate then send. node --check confirms handler.js parses clean (no syntax defects). This is a real send-blocking gate enforced at the code-execution boundary, not a UI-only label a determined caller could bypass.'
- id: opworkflow-case-assignment-durability
  obligation_kind: replay
  prd_id: operator-workflow-stability
  subject: 'Case assignment/field updates are durable under real concurrent writes: thatcher''s busybase-backed optimistic-lock (case-store.js updateCase/updateCaseChecked, expectedVersion) must reject a losing concurrent writer with a real conflict error, never silently drop the edit while reporting false success.'
  status: witnessed
  witness_evidence: 'Live exec_js witness against a REAL CaseStore + REAL thatcher (better-sqlite3-backed libsql via busybase, isolated scratch cwd per AGENTS.md Windows file-lock discipline, no mocks). FOUND A REAL BUG via Promise.all racing genuine concurrent writes: two operators writing the SAME field on the same case with the SAME expectedVersion (real TOCTOU race) both returned fulfilled -- the loser silently lost its edit while its own promise resolved reporting the winner data as if it were its own success (e.g. alice own updateCase call returned assignee=bob with no error). Root-caused to node_modules/thatcher/src/lib/busybase/store.js update(): the conflict check rows.length===0 trusts busybase rest/v1 PATCH handler (node_modules/busybase/src/server.js:559-582), which synthesizes its response body from the PRE-write existing snapshot plus the request patch rather than confirming the underlying conditional UPDATE actually touched a row (dbUpdate has no affected-row-count check at all) -- so the conflict signal never fires under real concurrency. Also found a second related gap: a freshly-created case row has _version=null until its first update(), during which window every case-store.js call site (before._version != null guard) skips the guard entirely -- two operators both editing a brand-new case at once silently clobber each other with zero conflict signal. FIXED in deps/thatcher/src/lib/busybase/store.js (commit c7c190b on deps/thatcher local main, NOT YET PUSHED per explicit task instruction to not push without authorization): (1) create() now stamps _version:0 on every insert closing the first-write race window; (2) update() now re-reads the row after every guarded write and compares the caller own requested field values plus target _version against what actually persisted -- any mismatch is a real conflict computed from live state, never trusted from busybase echo. POST-FIX LIVE RE-WITNESS (fix temporarily staged into node_modules/thatcher for verification only, then restored to original since not yet pushed): scenario A (2 concurrent writers, different fields, real Promise.all) -- exactly one succeeds, one gets a real conflict rejection, losing write never applied to DB, loser recovers via explicit retry against fresh version, both edits present after retry. Repeated 5x with consistent exactlyOneWon=true. Stress test: 10 concurrent same-field writers via real Promise.all -- exactly 1 fulfilled, 9 rejected all with code=conflict, winner matches final persisted state exactly. First-write race (brand-new case, never previously updated) re-tested post-fix: correctly guarded now (before._version reads 0 not null; loser conflicts correctly instead of silently losing data). Scenario B: 5 concurrent updateCaseChecked (the locked+auto-retry path used by case_tools.js) all eventually succeed via internal retry with no data loss and no unhandled rejection; final state is a clean single non-corrupted value. Scenario C regression guard: re-confirmed commit a5e1174 (sibling session stale-_version fix in case-store.js transition()) still holds -- returned _version matches an immediate independent read. Separately confirmed the dashboard PATCH /api/cases/:id route (src/dashboard/routes/cases.js:280) forwards expectedVersion correctly and has NO built-in retry by design (returns a clean 409 for the operator to reload+retry, documented in the route own comment) -- this satisfies every operator action is atomic or explicitly retried per the PRD invariant: the conflict is now a REAL, correctly-detected signal post-fix rather than a false-negative that silently dropped data. node --check confirms deps/thatcher/src/lib/busybase/store.js parses clean; diff is ASCII-only in all added lines (pre-existing unrelated em-dash on line 2 left untouched, out of scope). Fix committed to deps/thatcher local main as lanmower (no AI attribution), commit c7c190b -- requires push authorization plus npm install in casey before it reaches casey runtime, per AGENTS.md composed-dependency architecture; casey node_modules/thatcher was restored to its original unpatched state after the live-witness run, so casey runtime is unaffected pending that authorization.'
- session_id: casey-commitgate-sub-515723236
  id: mut-commitgate-pii-live-grep
  prd_id: commit-push-quality-gate
  obligation_kind: invariant
  subject: 'Live grep across src/dashboard/routes/*.js confirms no raw external_id leak: every occurrence is either a comment, an internally-used query field, or routed through caseListProjection()/publicContact(). contact_id (thatcher''s internal DB row id, distinct from external_id which is the actual phone/Discord PII) is confirmed by commit 70bf2c0''s own body as intentionally-not-PII.'
  witness_evidence: 'grep -n external_id|contact_id across src/dashboard/routes/*.js: 34 hits, manually reviewed each. cases.js/reports.js/operations.js/map.js hits are comments or internal query use; contacts.js:13 external_id_formatted is a formatted-phone projection gated by authed() for the internal operator triage panel (not worker-facing); map.js:146 contact_id:c.id is thatcher''s internal row id per commit 70bf2c0''s explicit rationale ("contact_id is internal database ID (not PII, used for map worker assignment)"). Corroborates scripts/lint.mjs''s pii-safety gate (lines 233-260) which scans for raw {...c} spread bypass patterns and independently reported clean.'
  status: witnessed
- session_id: casey-commitgate-sub-515723236
  id: mut-commitgate-ai-attribution-violation
  prd_id: commit-push-quality-gate
  obligation_kind: invariant
  status: witnessed
  subject: 'VIOLATION FOUND: 8 commits in main''s recent history carry a ''Co-Authored-By: Claude Haiku 4.5 <noreply@anthropic.com>'' trailer in their commit body, violating the never-attribute-claude invariant (CLAUDE.md: ''never attribute claude anywhere, only commit as lanmower''). Author field is correctly ''lanmower <almagestfraternite@gmail.com>'' on all of them -- only the body trailer is the violation.'
  witness_evidence: 'git log -20 --format=''=== %H ===%n%B'' | grep -inE ''claude|anthropic|co-authored-by'' found trailers in: dcaf88126e8fee7fdac11714096c47b35ede7bdf, 98a0264cc6a91abe4981aead50229df1bab36522, 196498ca1f40a792d849a4a6ace931745c06623f, 6322db9d54f0aa3d50db5deaa8d9f18ed0f4cca5, ab8531c815aefbc5319dafda1ef4f44478e7c640, 024672e6c84701caeaa959c0b54bf8064e771818, 70bf2c0efe19fd06a2b478133cd56b59516bf6b0, e1ac46ead3ec86a68212db14d2809d4ac6279228. This is a git-history-rewrite fix (filter-branch/rebase to strip trailers) which is a one-way door under Section 4 of gm''s own admission filter -- requires AskUserQuestion before any rewrite of already-pushed commits, not silently actionable by this verification-only PRD row.'
- session_id: casey-commitgate-sub-515723236
  id: mut-commitgate-lint-clean
  prd_id: commit-push-quality-gate
  obligation_kind: postcondition
  subject: node scripts/lint.mjs runs clean against the current tree (136 JS files, syntax+config+package+ascii+pure-agent+no-stub-mock+pii-safety all pass).
  witness_evidence: 'Live run: `node scripts/lint.mjs` -> stdout: "lint OK: 136 JS files syntax-checked, config + package + ascii + pure-agent + no-stub-mock + pii-safety clean", exit code 0.'
  status: witnessed
- session_id: casey-commitgate-sub-515723236
  id: mut-commitgate-standing-test-file
  prd_id: commit-push-quality-gate
  obligation_kind: postcondition
  status: witnessed
  subject: 'VIOLATION FOUND: verify-dashboard-state.mjs (377 lines, tracked, at repo root) is a standing E2E test harness (explicit Tests 1-7 structure, TEST_CASE_REF/OPERATOR_ID fixtures) added by commit dcaf8812, still present in the working tree. Violates AGENTS.md''s no-automated-test-suite invariant and gm''s no-standing-test-files-ever invariant.'
  witness_evidence: git ls-files shows verify-dashboard-state.mjs tracked. File head shows explicit numbered Tests 1-7 docblock and TEST_CASE_REF/OPERATOR_ID=test-verify-operator fixtures. File still present in working tree. This is a straightforward append-only-safe fix -- delete the file, new commit -- no history rewrite needed since the file is currently on disk in the live tree, not just in a past commit.
- session_id: casey-e2e-sub-7f3a
  id: e2e-golden-path-witness
  prd_id: end-to-end-witness
  obligation_kind: replay
  description: 'Live witness of contact->agent->case->operator->dashboard golden path against real running casey up (localhost:4000), real thatcher app.db, real dashboard auth. LLM provider credential confirmed absent (source:none, model:null, status:down via live GET /api/health and /api/health/provider) so the agent-turn/case_report-via-LLM portion is structurally unwitnessable in this environment -- disclosed honestly, not faked. Discord channel gateway is genuinely live-connected (sinceConnectMs>870000) but sending a real inbound message requires firing a live Discord bot message into an unscoped external server (world-scope reach into another system with no established test-channel authorization) and would not complete the golden path anyway since the LLM-down queue gate would only queue the turn, never reach case_report or a real reply -- so it was not attempted without user authorization. What WAS live-witnessed end-to-end via real HTTP against the real dashboard API (no mocks): case creation (POST /api/cases, 201, real case CASE-1029-NBFB3RUX id mt31sqa7-anvyc66l), report field merge via the SAME store.mergeReport() function case_report''s tool handler calls (POST /api/cases/:id/intake, 200, 7/28 fields filled), dashboard visibility (GET /api/cases/:id, 200, full event history visible), real operator transition (POST /api/cases/:id/transition new->triaging, 200, real _version bump 5->6, transition_reason recorded), real operator reply+claim (POST /api/cases/:id/reply, 200, delivered:true/claimed:true -- disclosed as web-channel no-op-adapter delivery semantics, not a real external channel send, since casey.adapters[''web''] does not exist and sendReply resolves silently per bin/worker.js:163-166), and full sync-back verification (GET /api/cases/:id/events, 200, 5 real audited event rows: case-created, report-recorded, transition, claim, outbound-reply, all timestamped and actor-attributed to the real authenticated operator).'
  depends_on: []
  status: witnessed
  witness_evidence: 'Real HTTP witness sequence (all timestamps epoch ms real, all HTTP 200/201): T=1787322716321 POST /api/cases -> 201, case id mt31sqa7-anvyc66l ref CASE-1029-NBFB3RUX created_at 1787322716. T=1787322730 POST /api/cases/mt31sqa7-anvyc66l/intake -> 200, report merged via store.mergeReport (species=cattle, symptoms, affected_count=3, onset, location, present_person, present_person_relation), fill_rate 7/28. T=1787322730 GET /api/cases/mt31sqa7-anvyc66l -> 200, dashboard-visible, 2 events at that point. T=1787322742 POST /api/cases/mt31sqa7-anvyc66l/transition {to:triaging} -> 200, real optimistic-lock _version bump 5->6, transition_reason persisted, status confirmed triaging. T=1787322784 POST /api/cases/mt31sqa7-anvyc66l/reply -> 200 {ok:true,sent:true,delivered:true,claimed:true}; claim event ''Claimed by E2E Witness'' + outbound event both real rows. Final GET /api/cases/mt31sqa7-anvyc66l/events -> 200, events_total 5, all rows real ids/timestamps/actor attribution (case-created mt31sqay-zhrx90eu@1787322716, report-recorded mt31t147-5ty7ewrq@1787322730, transition mt31tacr-zya3x4sy@1787322742, claim mt31u6oc-v2u8qd46@1787322784, outbound mt31u6pa-oemojsfu@1787322784). Auth: real login POST /api/login with throwaway scrypt-hashed operator_account (id e2ewit-mt31rak6), deleted post-witness (DELETE changes:1 confirmed). LLM/agent-turn portion honestly UNWITNESSED: live GET /api/health at T=1787322643 returned source:none, model:null, status:down (no reachable provider credential in .env despite CASEY_LLM_MODEL=auto); GET /api/health/provider corroborated status:down. Discord gateway confirmed genuinely live (channels.discord.state=ok, connected=true, sinceConnectMs=872736) but sinceInboundMs=null (never received live traffic this session) and a real inbound would only reach the LLM-down queue gate (queues silently, never reaches case_report or a real reply per handler.js''s documented STOP/HUMAN-short-circuit->LLM-down-queue-gate ordering) -- sending a live Discord message into an unscoped real external server was not attempted without explicit user authorization (world-scope boundary, Section 4). No mocks, no synthetic replies, no fabricated LLM output anywhere in this witness.'
- session_id: casey-concsweep-sub-7f3a9e
  obligation_kind: happens-before
  prd_id: casey-conc-sweep
  subject: 'src/casey.js drainQueuedTurns() re-entrancy guard (_draining) was NOT atomic: this._draining=true was set AFTER an awaited status-probe (await statusFn()), leaving a real TOCTOU window between the check and the claim. Two concurrent drainQueuedTurns() calls (the periodic drain-poll timer racing an LLM-recovery onRecover edge, or two recovery edges firing close together) could both pass the check before either claimed the guard, both entering the scan/redrive body concurrently -- risking duplicate queue-drive-attempted/queue-drive-retry event writes and a double-send of the same queued reply to a contact.'
  description: Live exec_js-equivalent witness (Node ESM, real src/casey.js Casey class, no mocks of the method under test) reproduced the race pre-fix (3/3 concurrent calls entered critical section) and confirmed the fix post-fix (1/3 entered, 2/3 deferred cleanly, guard resets correctly for a subsequent sequential call).
  depends_on: []
  id: mut-1787323841094
  status: witnessed
  witness_evidence: 'REAL BUG FOUND AND FIXED in src/casey.js''s drainQueuedTurns(). Pre-fix control flow: `if (this._draining) return {deferred:true}` (sync check) ... `await statusFn()` (status probe) ... `this._draining = true` (claim) -- the claim happened AFTER an await, so two concurrent invocations could both pass the check before either claimed the guard. LIVE REPRODUCTION (Node ESM, real control-flow shape mirroring casey.js lines 838-861 exactly, no mocks of the guard logic itself): Promise.all of 3 concurrent calls through the pre-fix shape yielded maxConcurrentEntries=3 (all three entered the critical section simultaneously) -- RACE_CONFIRMED=true. FIX: moved `this._draining = true` to be synchronous with the initial check (immediately after the `if (this._draining) return` and the `typeof handle` check, both sync, before the first await), wrapping the status-probe-and-scan body in a try/finally that resets `this._draining = false` on every exit path including the early degraded-status return (which now happens after the claim). Split the original scan/redrive body into a new private `_drainQueuedTurnsBody()` method called from inside the guarded section, so the guard''s own critical section is easy to see at a glance (claim -> status probe -> body -> release) without a ~160-line method body obscuring the guard shape. POST-FIX LIVE RE-WITNESS against the REAL casey.js Casey class (imported directly via file:// ESM import, with minimal store/gateway/adapter doubles supplying only the data the method reads -- no mock of drainQueuedTurns or its guard): Promise.all of 3 concurrent drainQueuedTurns() calls -> results=[{scanned:1,drained:1},{deferred:true},{deferred:true}], handleInboundCalls=1 (not 2 or 3 -- confirms no double-send of the same queued reply), maxConcurrentBodyEntries=1 (RACE_CLOSED=true, instrumented via a wrapper around the real _drainQueuedTurnsBody method). Sequential re-check after the race: a 4th, purely sequential call still correctly drains (scanned:1,drained:1) and `casey._draining` reads false afterward (GUARD_RESET_CORRECTLY=true) -- the fix does not leave the guard permanently stuck. Cross-checked resumePendingTurns (the sibling method sharing the same _draining guard, casey.js lines 581-592) as the CORRECT reference pattern -- its check-then-set has zero await between them, which is why it was never vulnerable to this class of bug; drainQueuedTurns had drifted from that pattern when the status-gate feature was added. Also live-witnessed case-sweep''s own re-entrancy guard (runSweepOnce''s _sweeping boolean, casey.js lines 393-394) as a SEPARATE, already-correctly-atomic guard (check-then-set with zero await between, matching resumePendingTurns'' shape) -- 3 concurrent runSweepOnce() calls: bodyRuns=1, RACE_CLOSED=true, 2 deferred cleanly, guard resets correctly. No fix needed there. Lint clean post-fix: node scripts/lint.mjs -> ''lint OK: 135 JS files syntax-checked, config + package + ascii + pure-agent + no-stub-mock + pii-safety clean''. node --check src/casey.js passes. Diff is ASCII-only, scoped entirely to the drainQueuedTurns/_drainQueuedTurnsBody split (45 insertions, 22 deletions, single file).'
- session_id: casey-concsweep-sub-7f3a9e
  obligation_kind: disjointness
  prd_id: casey-conc-sweep
  subject: 'src/hooks/handler.js''s per-contact inFlight guard (Set.has+Set.add, no await between) and src/case-store.js''s per-conversation _withLock (promise-chain lock keyed by channel|external_id, plus core/write-path.js''s identical per-subject mutex) are the two real single-writer surfaces protecting concurrent contact turns and store writes. Independently re-verified (not reused from a sibling session''s prior finding) that: (a) inFlight is genuinely atomic and correctly scoped per external_id (two different contacts never block each other); (b) _withLock genuinely serializes same-key access FIFO and lets disjoint keys run fully concurrently; (c) case-store.js mergeCases'' two-lock acquisition uses a sorted key order ([srcKey,tgtKey].sort()) which is the correct deadlock-avoidance pattern for a two-resource lock and was live-witnessed to never deadlock even when two merges race in opposite key order.'
  description: Live exec_js-equivalent witness (Node ESM, real makeCaseHandler and real CaseStore.prototype._withLock, no mocks of the guards themselves) covering happens-before, disjointness, and deadlock-avoidance for these three mechanisms.
  depends_on: []
  id: mut-1787323906023
  status: witnessed
  witness_evidence: 'Fresh, independent live witness (not reused from casey-edgecases-sub-7f3d21''s already-witnessed burst-messaging finding, though it corroborates it) against the REAL src/hooks/handler.js makeCaseHandler: constructed real gateway-shaped `this` objects (handleInbound bound to itself, mirroring casey.js''s real wiring per the handler''s own comment). Same-contact test: 3 concurrent handleInbound calls for contact-A -- peakConcurrentStoreEntries into findOrCreateCase stayed at 1 (never 2+), 1 processed synchronously + 2 buffered (HAPPENS_BEFORE_HOLDS=true); repeated for contact-B with identical result. Cross-contact test: contact-A and contact-B racing concurrently via Promise.all -- both proceeded without either blocking the other (DISJOINTNESS_HOLDS=true), confirming the guard is scoped per external_id key, not a single global lock. Separately, live-witnessed src/case-store.js''s CaseStore.prototype._withLock directly (Object.create + real method, no mock): (1) 3 concurrent _withLock calls on the SAME key (''convo:1'') ran strictly one at a time in FIFO submission order (maxConcurrentSameKey=1, order=[a,b,c], SERIALIZED_CORRECTLY=true); (2) 3 concurrent calls on 3 DIFFERENT keys ran fully concurrently (maxConcurrentDiffKey=3, DISJOINTNESS_HOLDS=true); (3) mergeCases'' own two-lock deadlock-avoidance shape ([keyA,keyB].sort() before acquiring both) was replicated and raced: two ''merge'' operations attempting case:A+case:B in OPPOSITE submission order (A-into-B vs B-into-A) both completed in 39ms against a 2000ms deadlock-timeout race (NO_DEADLOCK=true) -- proving the sorted-key-ordering pattern genuinely prevents the classic two-resource deadlock a naive un-sorted double-lock would hit. _locks Map confirmed empty (no leak) after all operations settled. core/write-path.js''s per-subject mutex uses the byte-identical pattern (confirmed via source read, own comment explicitly states ''mirroring case-store.js''s own _withLock'') and was already independently live-witnessed correct by a sibling session (data-provenance-writeobservation-rank-tracking mutable: 10 concurrent writeObservation calls to the same subject serialized correctly, 0 corrupt lines) -- not re-witnessed here to avoid duplicate work, cross-referenced instead. No fix needed anywhere in this surface -- both mechanisms are correctly atomic and correctly scoped as designed. node scripts/lint.mjs clean (no code changes made for this mutable, verification-only).'
- session_id: casey-concsweep-sub-7f3a9e
  obligation_kind: contention
  prd_id: casey-conc-sweep
  subject: src/casey.js's runSweepOnce() re-entrancy guard (_sweeping boolean) is genuinely atomic (check-then-set with zero await between, correctly closing the TOCTOU window drainQueuedTurns had). Its downstream _checkCoverageGap()'s unlocked _coverageGapActive read-then-set (flagged as a risk in the method's own comment) is safe by construction because _checkCoverageGap has exactly one call site, inside runSweepOnce's own _sweeping-guarded section -- it can never be invoked concurrently with itself. Separately, contention bounds documented in AGENTS.md (CASEY_RESUME_MAX_REDRIVES, CASEY_RESUME_SPACING_MS with jitter, RESUME_DEGRADED_RETRY_CAP=5, CASEY_RESUME_MAX_AGE_MS, case-level resume-exhausted dead-letter, drainQueuedTurns' retryCap=5 dead-letter, maxCases=200 scan bound) are all real, enforced in code, not merely documented prose.
  description: Live exec_js-equivalent witness (Node ESM, real Casey.prototype.runSweepOnce, no mock of the guard) plus direct source-read confirmation of every bound's enforcement point in resumePendingTurns/drainQueuedTurns.
  depends_on: []
  id: mut-1787323964042
  status: witnessed
  witness_evidence: 'Live exec_js-equivalent witness (Node ESM, real Casey.prototype.runSweepOnce, minimal store double supplying only data the method reads, no mock of runSweepOnce or _sweeping itself): 3 concurrent runSweepOnce() calls -> bodyRuns=1 (instrumented via wrapping store.resolveThresholds, the first awaited call inside the guarded section), RACE_CLOSED=true, results=[{scanned:0,flagged:0,...real summary shape},{deferred:true},{deferred:true}]. A 4th sequential call after the race still ran correctly and casey._sweeping read false afterward (GUARD_RESET=true) -- confirms _sweeping is genuinely atomic (check-then-set with zero await between them, matching resumePendingTurns'' correct shape, unlike drainQueuedTurns'' pre-fix shape). Source-confirmed _checkCoverageGap (casey.js line 438) has exactly one call site (line 426, inside runSweepOnce''s own try block, after this._sweeping=true) via grep across src/ -- structurally cannot be invoked concurrently with itself, so its own comment-flagged ''unlocked _coverageGapActive read-then-set'' is safe by construction, not a live gap. CONTENTION BOUNDS -- confirmed enforced in code via direct source read of resumePendingTurns/drainQueuedTurns (casey.js): maxRedrives (default CASEY_RESUME_MAX_REDRIVES=10) checked at ''if (resumed >= maxRedrives) break'' inside the per-case loop; spacingMs (default CASEY_RESUME_SPACING_MS=2000) applied with +/-20% jitter (''spacingMs * (0.8 + Math.random()*0.4)'') before each non-first redrive, explicitly to avoid synchronizing retries into a thundering herd against shared rate-limited providers; RESUME_DEGRADED_RETRY_CAP=5 (hardcoded constant, not tunable) bounds per-msgId degraded-retry count across all boots, checked via ''degraded >= RESUME_DEGRADED_RETRY_CAP''; RESUME_MAX_AGE_MS (default CASEY_RESUME_MAX_AGE_MS=24h) bounds age via ''ageMs >= RESUME_MAX_AGE_MS'', same disjunctive check as the retry cap; case-level ''resume-exhausted'' tag (set once anyCapped is true and no pending msgId survives) short-circuits ALL future scans of that case at the top of the per-case loop (''if (tagList(c).includes("resume-exhausted")) continue''), preventing the multi-msgId starvation bug the code''s own comment describes being live-witnessed and fixed in a prior session; drainQueuedTurns'' retryCap=5 (default param) bounds per-msgId drive retries with an identical dead-letter-on-exhaustion pattern (data.dead_lettered:true); maxCases=200 bounds the per-sweep case scan in both methods. All bounds are real code-enforced ceilings, not aspirational AGENTS.md prose alone. No fix needed anywhere in this surface. node scripts/lint.mjs clean (verification-only, no code changes for this mutable).'
- session_id: gmsess-uhh
  id: mut-config-file-format
  subject: 'Config package file format: single YAML manifest vs split files (prompt.yml, report-fields.yml, thatcher-entities.yml, dashboard.yml) vs a JS module exporting an object.'
  obligation_kind: type-shape
  prd_id: design-config-schema
  status: witnessed
  witness_evidence: 'DEFAULT (agent judgment, disclosed): config package is a directory with a manifest casey.config.js (JS module, since the prompt template needs template functions not just static data) that imports split declarative files -- report-fields.yml (replaces store/report-shape.js REPORT_KEYS/REPORT_KEY_ORDER), thatcher-entities.yml (feeds thatcher.config.yml generation), dashboard.yml (branding/labels), persona.js (prompt template strings + priority-order list). Mirrors the existing pattern where thatcher.config.yml (declarative) already sits alongside JS code (case-tools.js) in this same codebase -- consistent with what is already proven to work here, not a novel pattern.'
- session_id: gmsess-uhh
  id: mut-config-dir-env-name
  subject: Exact env var name/contract for pointing casey at an external config dir (CASEY_CONFIG_DIR candidate) -- must not collide with existing CASEY_* vars in AGENTS.md Environment table, and must be documented there per docs-agents-md-update row.
  obligation_kind: invariant
  prd_id: casey-config-loader
  status: witnessed
  witness_evidence: 'DEFAULT (agent judgment, disclosed): CASEY_CONFIG_DIR -- checked against AGENTS.md Environment table (WHATSAPP_APP_SECRET, CASEY_SESSION_SECRET, CASEY_OPERATORS, CASEY_LLM_MODEL, CASEY_TZ/CASEY_TZ_LABEL/CASEY_COUNTRY_CODE, CASEY_TRANSCRIBE_VOICE_NOTES, CASEY_DESCRIBE_PHOTOS, CASEY_VOICE_REPLIES, CASEY_LOCATION_STALE_MS, CASEY_MEDIA_TOOL_TIMEOUT_MS, CASEY_TRUST_PROXY_HOPS, CASEY_MINE_SCAN_LIMIT, CASEY_MIN_AGGREGATE_CELL, CASEY_AUTO_UPDATE, CASEY_DRAIN_DEADLINE_MS, CASEY_DRAIN_TURN_TIMEOUT_MS, CASEY_RESUME_*, CASEY_DRAIN_POLL_INTERVAL_MS, CASEY_SWEEP_INTERVAL_MS, CASEY_HEALTH_BREACH_WINDOW_MS, CASEY_RATE_LIMIT_*, CASEY_TURN_HARD/SOFT_DEADLINE_MS, CASEY_PUBLIC_URL, CASEY_PROMPT_COHORT_PERCENT, CASEY_PROMPT_VARIANT) -- no collision. Absent = falls back to a bundled default (generic-demo) config directory in the casey repo itself, never a hard requirement.'
- session_id: gmsess-uhh
  id: mut-uhh-casey-resolution
  subject: 'How uhh resolves+installs casey at npx-run time: npm install github:AnEntrypoint/casey#main into a temp/cache dir vs npx github:AnEntrypoint/casey directly vs casey itself becomes an installable dependency of uhh in package.json.'
  obligation_kind: precondition
  prd_id: uhh-npx-bootstrap
  status: witnessed
  witness_evidence: 'DEFAULT (agent judgment, disclosed): uhh''s package.json declares casey as a real npm dependency via github:AnEntrypoint/casey#main (identical spec shape to casey''s own 4 composed deps per AGENTS.md), so `npx github:AnEntrypoint/uhh` triggers npm to resolve+install casey into uhh''s own node_modules; uhh''s bin script then requires casey''s CLI programmatically (or shells to node_modules/.bin/casey) with CASEY_CONFIG_DIR pointed at uhh''s own bundled config directory before invoking `up`.'
- session_id: gmsess-serp
  id: mut-serpent-research-run-shape
  subject: What IS a 'research run' concretely -- what fields/entities does a generic research-tracking domain need (hypothesis, dataset, experiment, result, researcher role?) vs casey's existing case/event/contact shape. Needed to design serpent's report-fields.yml/thatcher.config.yml.
  obligation_kind: precondition
  status: witnessed
  witness_evidence: 'User (AskUserQuestion): option 1, generic experiment tracking, explicitly minimal/no over-engineering (''no chekovs gnns about specific fields''). Fields: hypothesis, method, dataset_or_sample, result, status (planned/running/done/failed), researcher. Domain-agnostic, not specific to any one research field.'
- session_id: gmsess-serp
  id: mut-serpent-dynamic-gui-meaning
  subject: 'What ''dynamic thatcher gui for any research run'' means precisely: (a) the dashboard renders fields/sections purely from config with zero hardcoded field names (already true after last session''s report-sections.js work), or (b) something further -- e.g. per-run schema that can differ WITHIN one deployment (multiple concurrent research runs each with their own field set), which today''s one-config-per-process model does not support.'
  obligation_kind: precondition
  status: witnessed
  witness_evidence: 'User (AskUserQuestion): runtime-dynamic -- ONE running serpent instance must handle MULTIPLE concurrent research runs, each with a potentially different field schema, live at the same time. Not a per-process CASEY_CONFIG_DIR pick. Requires: config resolution to move from module-level/process-lifetime (report-shape.js''s current cached-at-import-time design) to per-run/per-request, with the schema for each run stored alongside the run itself (in thatcher) rather than only in a filesystem config directory.'
- session_id: gmsess-serp
  id: mut-serpent-multiagent-mechanism
  subject: 'Concrete mechanism for ''freestyle non linear long horizon multi agent consolidated research runs that build a body of notes in a folder, and consolidates them en masse'': what dispatches the multiple agents (gm''s own Workflow/Agent tooling used AS this session drives serpent, vs a NEW dispatch mechanism serpent''s own runtime must implement independently of any interactive session), what triggers consolidation (a schedule, a manual operator action, a run-status transition to done), where the notes folder lives relative to a research_run row (one folder per run id?), and what ''consolidates them en masse'' produces (a single merged report field, a new thatcher event, a separate summary entity).'
  obligation_kind: precondition
  status: witnessed
  witness_evidence: 'User: freddie itself should gain workflow support (''../freddie should have workflow support to a degree we can perfect any part of it change any part you need to make it as useful as possible'') -- NOT gm''s own external Workflow tooling, real engineering in the freddie submodule. Notes-folder mechanism: one folder per research_run named by ref/id, consolidation is ON-DEMAND (a triggered action -- CLI/tool/dashboard button -- not scheduled/automatic). Existing freddie infra found: batch.js (N independent parallel runTurn calls, journaled+resumable, but no fan-out/coordination/handoff between agents), machines/persistent-actor.js + step-journal.js + resume.js + snapshot-store.js (long-horizon resumable single-turn infra already exists). Gap: freddie has no multi-agent FAN-OUT-THEN-CONSOLIDATE primitive -- batch.js runs N independent prompts to N independent results, never N agents contributing to ONE shared artifact (a notes folder) that a later step reads back.'
- session_id: gmsess-serp
  id: mut-serpent-adversarial-review-mechanism
  subject: '''As much adversarial review as possible'' -- is this (a) a development-process instruction for THIS build (already covered by gm''s own DECIDE-phase adversarial sweep, no new serpent code needed), or (b) a RUNTIME feature of serpent itself -- research findings/notes get adversarially reviewed by other agents as part of the research workflow (e.g. a refute-only reviewer agent runs against every consolidated note before it''s accepted), needing real code (a review-dispatch mechanism, a verdict field, a review-pending workflow stage).'
  obligation_kind: precondition
  status: witnessed
  witness_evidence: 'Inferred from context: user''s ''as much adversarial review as possible'' sits alongside ''freestyle non linear long horizon multi agent consolidated research runs'' as a description of the RESEARCH WORKFLOW itself, not a development-process instruction (which was already answered separately in the prior AskUserQuestion turn, before this expanded answer). Treating as: a runtime feature -- the consolidation step (or a dedicated review agent) should adversarially review contributed notes before/as part of consolidating them, mirroring the refute-only reviewer pattern already used in this session''s own gm DECIDE-phase sweeps. Concrete design: consolidation dispatches N independent refute-only reviewer turns against the draft consolidated summary (same pattern as gm''s own adversarial-review workflow shape), survivor findings only are kept.'
- session_id: gmsess-serp
  id: mut-serpent-spa-not-wired
  subject: 'Real gap found during gm-continue''s re-verification pass: casey''s dashboard SPA (report-sections.js) reads state.config, populated ONLY from casey''s own global GET /api/config fetched once at page load. Serpent''s per-run dynamic config lives at a SEPARATE endpoint (GET /api/runs/:id/config on serpent''s own server.js, a different port) that the SPA has no code path to call. The API/data layer is proven correct (live-verified two runs resolving different schemas), but the actual browser-rendered GUI, if opened against serpent today, would still show the static global config for every run, not the per-run dynamic one -- the literal ''dynamic thatcher GUI for any research run'' claim is not yet met at the UI-rendering layer.'
  obligation_kind: postcondition
  status: witnessed
  witness_evidence: 'Read casey source directly: dashboard/server.js createDashboard already resolves {app, server, port, close} and bin/worker.js already holds dash.app after calling createDashboard. Prior assumption in serpent/src/server.js header comment that createDashboard does not expose app is false, contradicted by direct source read. Real fix: extension point in bin/worker.js mounting deployer routes onto the real dash.app after createDashboard resolves, mirroring the shipped CASEY_EXTRA_PLUGINS_DIR pattern, so serpent per-run routes share casey dashboard origin/port and the SPA same-origin fetch works without a separate port or proxy.'
