---
key: mem-a62627a4f642aafe-807
ns: default
created: 1787317261397
updated: 1787317261397
---

## Resolved mutable: obs-health-cases-per-operator

Added assignee field to each case row and a by_operator rollup array (operator, case_count, breach_count, sorted worst-first) to GET /api/health/cases (operations.js), satisfying the PRD's explicit post-condition 'GET /api/health/cases returns case-level health signals per operator'. Live HTTP witness: real case created and assigned via store.updateCaseQuiet(id,{assignee:'admin'}); GET /api/health/cases response included by_operator:[] (correctly empty since the test case had no active health breach) and every case row in the cases array carries the assignee field (assertion 'cases[].assignee field present' passed). Field is an operator username, never PII (no external_id/contact_id), consistent with /api/attention's existing assignee exposure.
