---
key: mem-88e53b2114826f79-1029
ns: default
created: 1787654123127
updated: 1787654123127
---

## Resolved mutable: mut-serpent-multiagent-mechanism

User: freddie itself should gain workflow support ('../freddie should have workflow support to a degree we can perfect any part of it change any part you need to make it as useful as possible') -- NOT gm's own external Workflow tooling, real engineering in the freddie submodule. Notes-folder mechanism: one folder per research_run named by ref/id, consolidation is ON-DEMAND (a triggered action -- CLI/tool/dashboard button -- not scheduled/automatic). Existing freddie infra found: batch.js (N independent parallel runTurn calls, journaled+resumable, but no fan-out/coordination/handoff between agents), machines/persistent-actor.js + step-journal.js + resume.js + snapshot-store.js (long-horizon resumable single-turn infra already exists). Gap: freddie has no multi-agent FAN-OUT-THEN-CONSOLIDATE primitive -- batch.js runs N independent prompts to N independent results, never N agents contributing to ONE shared artifact (a notes folder) that a later step reads back.
