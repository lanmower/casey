---
key: mem-a3f82dc069ea0d14-988
ns: default
created: 1787321593183
updated: 1787321593183
---

## Resolved mutable: mut-1787321458131

node live exec: constructed a store double whose getContact(id) returns a mutable currentTier variable. Called store.getContact once (tier=reporter, resolvedTier=reporter matching handler.js's tier-ternary expression: contact tier field_worker check falls back to reporter), flipped currentTier to field_worker (simulating an operator promotion), called store.getContact again -- resolvedTier immediately reflects field_worker. Source-confirmed handler.js: the contact variable is read via store.getContact(fresh.contact_id) unconditionally at the top of every handleInboundOnceClaimed call (no module-level or per-contact cache variable), and resolvedTier (around line 857) is recomputed inside the per-ATTEMPT retry loop from that same fresh contact object, so even a retried attempt within one turn re-derives tier from the same fresh read. No code change needed -- mechanism already correct, no restart required for a promotion to take effect.
