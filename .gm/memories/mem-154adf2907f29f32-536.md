---
key: mem-154adf2907f29f32-536
ns: default
created: 1787597017418
updated: 1787597017418
---

## Resolved mutable: mut-uhh-casey-resolution

DEFAULT (agent judgment, disclosed): uhh's package.json declares casey as a real npm dependency via github:AnEntrypoint/casey#main (identical spec shape to casey's own 4 composed deps per AGENTS.md), so `npx github:AnEntrypoint/uhh` triggers npm to resolve+install casey into uhh's own node_modules; uhh's bin script then requires casey's CLI programmatically (or shells to node_modules/.bin/casey) with CASEY_CONFIG_DIR pointed at uhh's own bundled config directory before invoking `up`.
