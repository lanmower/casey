---
key: mem-1a71417a89374c85-657
ns: default
created: 1787317257506
updated: 1787317257506
---

## Resolved mutable: obs-timestamp-seconds-vs-ms

Replaced new Date(event.created_at).getTime() with tsMs(event.created_at) (imported from timestamp.js, the shared digit-string-aware parser already used by attn.js/case-health.js/case-sweep.js) in both queryDegradedTurns and calculateDegradationRate. Live HTTP witness: real thatcher-created events (created_at stamped as unix-seconds integer per case-store.js's own appendEvent, e.g. 1787316712) were correctly windowed by GET /api/turns/degraded's since= filter and surfaced with count:2, proving the seconds-vs-ms conversion is now correct against real thatcher-stamped rows, not a synthetic ISO string.
