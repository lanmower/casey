---
key: mem-c2d3980b44df68cc-1129
ns: default
created: 1787321594809
updated: 1787321594809
---

## Resolved mutable: mut-1787321570782

Read case-tools.js in full: REPORT_ONLY_TOOLS = new Set of case_report, case_stop, case_handoff, case_new (line 862) is explicitly documented as available at every tier, including the default reporter tier -- comment states verbatim that case_new (opening a fresh case for a genuinely new situation) applies the never-a-dead-end design principle to every reporter, not only field workers. gateByTier(tool) (line 886) only wraps tools NOT in REPORT_ONLY_TOOLS with the field_worker-tier check; case_new returns unwrapped (line 887: if REPORT_ONLY_TOOLS.has(tool.name) return tool), so it always reaches the handler regardless of ctx.tier. case_new's own handler (lines 639-666) has no deterministic conflict/reuse-forcing logic -- it unconditionally creates a new case via store.createCase and rebinds the turn's active case (rebindActiveCase), matching AGENTS.md's stated design that there is no deterministic conflict detector, field-merge is fill-if-empty, and a contact who has clearly moved on is entirely the agent's judgment call. No code change needed -- mechanism already correct.
