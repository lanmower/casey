---
key: mem-dfdfb94913d7e848-667
ns: default
created: 1787321591031
updated: 1787321591031
---

## Resolved mutable: mut-1787321445824

grep-confirmed 2 real call sites: bin/worker.js:125 wires makeResilientCallLLM's onRecover callback directly to caseyRef.drainQueuedTurns().catch(...); casey.js:531 runs an independent periodic drain-poll (startDrainPoll, CASEY_DRAIN_POLL_INTERVAL_MS-gated) that also calls this.drainQueuedTurns() so a queued contact is not solely dependent on a new inbound arriving to trigger drain. This matches mutable mut-provider-chain-queue-drain (session casey-provsub-2216, status witnessed) already in .gm/mutables.yml, which live-witnessed the down/recover/drain/deliver cycle against the real llm.js module. No further fix needed.
